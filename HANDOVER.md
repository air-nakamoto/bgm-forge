# BGM Forge V2 引き継ぎ資料

最終更新: 2026-09-20
この文書だけ読めば、別のチャットで作業を続けられるように書いてあります。

**AIに作業を頼むときは `AGENTS.md` を先に読ませてください。** 開発のループ（測る→直す→測り直す→
テストに固定する→記録する）、触ってよいファイル、Gitの決まり、過去に踏んだ落とし穴をまとめてあります。
この文書は経緯と計測値の記録です。

**置き場所が変わりました。** 2026-09-17にGitリポジトリ（`air-nakamoto/bgm-forge`）へ移り、
現在は Cloudflare Workers Static Assets で公開しています。旧作業フォルダ `inside-rooms` は凍結扱いです。

- 作業フォルダ: `~/Documents/Codex/2026-09-17/documents-plugin-documents-openai-primary-runtime/work/bgm-forge`
- 公開: https://bgm-forge.suihei.workers.dev/
- 公開手順: リポジトリ直下の `公開する.command` をダブルクリック

---

## 0. 30秒でわかる現状

### 今回の到達点（2026-09-20 夜）

- 実装は **24場面・17音色**。この日に 🌊水辺 を足して 6列×4行 になり、**並びに規則を入れた**
  （行ごとに 日常／旅／情緒／緊張、**行の中は左から右が物語の順**＝序盤→終盤）。
- **同梱音源のうち4種が実録音になった**（すべてCC0、出典はページで再確認済み）。
  シタール＝Freesound 42192、箏＝365242、合唱＝764124、笛＝553217。`samples/<名前>/` に
  `bank.js` / `manifest.json` / `CREDITS.md`、再生成は `scripts/build_<名前>.py`。
  原音は `.gitignore` 済み（箏の原音は448MBあり、コミットするとpushごと失敗する）。
- **和風の「ぴーん」は消えた**（耳で確認済み・採用）。原因は2つ。(1) 箏を音源の上限より
  +7半音を超えて早回ししていた（`SAMPLE_LIFT` で折り返し、最大17.6→6.5半音）。
  (2) **同梱音源の音高表示が最大18.7半音ずれ、倍音を持たない細い音が旋律の56%を占めていた**。
  `scripts/build_koto.py` を検算つきに作り直した。音源の音高はテストが毎回実測する。
- **神楽に笛と鈴が入った**。笛は実録音（ループ点つき）、鈴は合成（`SUZU` 表）。
  「笛がきこえない」は音量ではなく発音経路の問題で、`LEAD_LIFT` と打楽器を下げて解決した。
- **民族をオリエンタル寄りにした**。ヒジャーズ（`phrygianDominant`）、持続低音、内声のアルペジオ。
  シタールの「じゃらん」は4回直したすえ**撤去**（§6.0 の「却下されたこと」）。
- **オルゴールを作り直した**。櫛歯は棒の振動なので、整数倍音ではなく 1 : 2.76 : 5.40 : 8.90 で積む。
- **画面まわり**：一番下に「ページの一番上に戻る」、クレジットに出典リンク（笛が丸ごと抜けていた）、
  権利の2行を折りたたみの外へ、決断の説明に「シリアス」、神楽の説明から「儀式」を外した。
- **「意見を送る」を足した**（2026-09-20 最後の作業）。Cloudflare Worker 経由でDiscordへ。
  **Workerの公開とページへのURL設定は完了**。DiscordのWebhook登録も完了。ページの公開と実送信確認が残っている（手順は §6.0）。
  **単体起動版には入らない**（ビルドが丸ごと落とし、痕跡ゼロをアサート）。
- 検証は **816ケースPASS**。単体版と分割ソースの一致、`?v=` のハッシュ照合、音源の実測音高、
  旋律の跳躍の上限、場面ごとの伴奏の相異、単体版に送信機能が無いことをテストが見ている。
- 公開は `公開する.command`。Cloudflareへデプロイ後、ソースをGitHubへpushする。
- **次にやること**は §6.0 にまとめてある。音の話はすべて「鳴らして確かめる」が残っている。


ブラウザ単体で動くループBGM自動作曲ツール。アカウント不要。**作曲・再生・保存はネット接続なしで動く**
（唯一の外向き通信は「意見を送る」を押したときだけで、単体起動版にはその機能自体が入らない）。
TRPG（ココフォリア）のセッション中に流す用途が主目的で、**既定は「伴奏だけ・ループ用・合成音源」**。

動かし方は2通りある。中身は同じで、配り方だけが違う。

| ファイル | 使う場面 | 実体 |
|---|---|---|
| `bgm_forge_standalone.html` | 人に渡す・別のPCで開く | 音源もMP3エンコーダーも埋め込んだ1ファイル（20.8MB）。これ単体で全機能が動く |
| `bgm_forge.html` | **開発するとき** | HTML＋JS＋`samples/`＋`vendor/` の分割構成。同じフォルダごと開く |

**編集は必ず分割ソース側に入れ、`python3 scripts/build_standalone.py` で単体版を作り直すこと。**
単体版を直接編集すると分割ソースと中身がずれる（実際に2026-09-16までずれていた。§3 第4次）。

- 検証: `node tests/scene-variation.cjs`（816ケース。PASSを維持すること）
- 旧フォルダにあった score / playback / adjust / browser / loop / seam の各テストは**リポジトリへ移していません**。
  必要になったら `inside-rooms/tests/` から持ってくること（§5）。
- 見た目の確認: ヘッドレスChromiumで `bgm_forge.html` を開いてスクリーンショットを撮る方法が有効だった（§4）

**画面の流れ**: 場面を選ぶ → 新しい曲を作る → 聴く → （調整して作り直す）→ 保存。
いま操作できる場所の枠が光る（複数同時に光る）。音色・テンポ・メロディ・打楽器は、変えると2小節だけ自動で試聴が鳴る。

**編集時の注意**: `bgm_forge.html` の `<script src="...?v=YYYYMMDD-名前-ハッシュ">` の版を、JSを変えるたびに上げること。
末尾8桁はそのファイルのsha256の先頭8桁で、**手で計算する必要はない**。合っていなければテストが落ち、
貼るべき文字列をそのまま表示する。
`python3 -m http.server` はキャッシュ制御を返さないため、上げないとブラウザが古いJSを使い続け、修正が反映されない。
単体版にはこの問題はない（埋め込みなのでキャッシュの対象にならない）が、作り直しを忘れると古いままになる。

---

## 1. ファイル構成

| ファイル | 役割 | 触る頻度 |
|---|---|---|
| `bgm_forge.html` | 画面とCSS。UI要素のidはここが正 | 高 |
| `bgm_forge_standalone.html` | 単体起動版。**生成物なので直接編集しない** | ビルド時のみ |
| `favicon.svg` | タブ用アイコン。単体版へビルド時に埋め込み | 低 |
| `ogp.svg` / `ogp.png` | 共有画像の編集元 / 公開用PNG | 低 |
| `scripts/build_standalone.py` | 分割ソース→単体版のビルド。クレジットの同梱もここ | 低 |
| `bgm-forge.js` | 雰囲気/テンポ/音源の定義、音声合成、再生、保存、UI配線 | 高 |
| `bgm-score.js` | **作曲の中核**。純粋関数のみ。音を出さない | 高 |
| `samples/vsco2/` | VSCO 2 CE（CC0）の音源32サンプル。`bank.js` が約12.9MB | なし |
| `samples/sitar/` `koto/` `shinobue/` `choir/` | CC0の実録音。各0.6〜0.8MB。`manifest.json` に実測音高 | 低 |
| `scripts/build_sitar.py` ほか3本 | 原音から同梱バンクを作り直す。原音は非収録 | 低 |
| `worker/` | 「意見を送る」をDiscordへ中継する Cloudflare Worker。**分割版だけの機能** | 低 |
| `vendor/lamejs/` | MP3エンコーダ。`lamejs-1.2.1.tgz` はLGPL用の原本一式 | なし |
| `tests/scene-variation.cjs` | 816ケースの回帰テスト。**仕様の実体はここ** | 高 |
| `README.md` | 利用者向け説明 | 中 |
| `AGENTS.md` | **AI向けの開発ルール。着手前に読ませる** | 中 |
| `LICENSE` | コードのMIT。生成物はCC0相当（README参照） | なし |
| `公開する.command` | ダブルクリックで `git push` する補助。場所に依存しない | なし |
| `HANDOVER.md` | この文書 | 中 |

配布は `bgm_forge_standalone.html` を1つ渡すだけでよい。分割構成のまま配るなら
`bgm_forge.html` / `bgm-forge.js` / `bgm-score.js` / `favicon.svg` / `samples/vsco2/` / `vendor/lamejs/` を同じ相対配置でコピーする。

---

## 2. 設計の要点

### 2.1 作曲と音声の分離

`bgm-score.js` は**音を一切出さない純粋関数**。`events(score)` が返すノートイベント配列を、
音声レンダリングとMIDI書き出しの**両方が同じものを使う**。これが壊れると音とMIDIがずれるので、
テストで `audio/MIDI event parity` を検査している。

```
compose(settings, seed) → score          … 調・進行・モチーフ・旋律を決める（決定的）
events(score)           → [{part,pitch,beat,duration,velocity,pan}]
remix(score,kind,seed)  → score'         … 旋律を保ったまま編曲だけ変える
scene(score,id,seed)    → score'         … 同じテーマを探索/不穏/戦闘に仕立て直す
```

パート番号: `0`=旋律 `1`=パッド `2`=ベース `3`=内声（分散和音） `4`=打楽器

### 2.2 決定性

同じ `seed` と同じ設定なら、必ず同じ `fingerprint` になる。乱数は `rng(seed)` のみで、
`Math.random()` は使っていない（合成ノイズも種から作る）。
`arrangementSeed` は編曲だけを司り、`remix('accompaniment')` で進める。

### 2.3 音域の住み分け（`bgm-score.js` 冒頭の定数）

```js
const INNER_GAP=4, INNER_SPAN=11, PAD_LOW=55, PAD_HIGH=79;
```

- 旋律は本来の音域（60+root+...）のまま動かさない
- **内声は旋律の最低音より必ず4半音下**の1オクターブ内に置く（`INNER_GAP`）
- パッドは同梱の弦サンプルの音域 [55,79] に収める
- テストで「旋律の最低音 ≥ 内声の最高音 + INNER_GAP」を検査

旋律を上げるのではなく内声を下げる方式にしたのは、旋律を上げると音源の最高音（C6=84）を超えて痩せるため。

### 2.4 テーマ長は尺から自動決定

`themeBarsFor(bpm,length)` が 2/4/8/16小節を返す。尺に入りきらないテーマ長は選ばない（テストで全網羅）。
46BPM×20秒だけ2小節になる（I–Vのヴァンプ）。気になるなら20秒の選択肢を25秒にすれば全テンポで4小節以上。

---

### 2.5 パッドは転回ではなく声部進行で決める

`voiceChord(pitches, previous, next)` が、和音の各構成音を `[PAD_LOW, PAD_HIGH]` の中の
どのオクターブに置くかを総当たりで探す。評価は「前の和音からの移動量×2 ＋ 次の和音への移動量
＋ 広がりすぎ/狭すぎの罰 ＋ 低音域での2度未満の罰」。ループの最終小節は先頭の和音を `next` に渡すので、
折り返しでも声部がつながる。

種から転回形を選ぶ以前の方式と比べ、和音1つあたりの移動 1.84→0.68半音、音域外 3→0音、
和音の広がり 8.54→11.85半音（336曲・3610和音で計測）。

### 2.6 試聴は `previewBars`、テーマ長は触らない

2小節試聴は `previewBars=2` を立てるだけで、`themeBars` は元のまま。以前は `themeBars` を
2に書き換えていたため、試聴のたびにテーマの形そのものが変わっていた。
`previewBars` が立っている間はループのターンアラウンドも作らない（2小節に折り返しは要らない）。

### 2.6b 場面ごとの編成（`STYLES`）

`bgm-score.js` の `STYLES` が、場面ごとに「どのパートを鳴らすか」と「どの伴奏型を使うか」を決める。
`pad:false` / `inner:false` でパートを落とし、配列は figure の重み表を差し替える。
場面は `mood.style`（`bgm-forge.js`）で編成名を持ち、`compose()` が `s.style` として持ち越す。

これを入れる前は、旋法と和音進行が場面ごとに違っても**伴奏の密度が全18場面でほぼ同一**だった
（パッド cv 0.056 / 内声 0.084 / ベース 0.070）。それが「どれも同じ曲に聞こえる」の主因。
入れた後は 0.464 / 0.407 / 0.192。`tests/adjust.test.cjs` が下限を守る。

### 2.7 打楽器の揺れ（`humanize`）

`events()` の `add()` が part 4 のときだけ、強弱とタイミングを揺らす。乱数は他の編曲判断と混ざらない
専用の系列（`rng((arrangementSeed||1)*7+13)`）なので、揺れを足しても既存のパターン選択は変わらない。

```
タイミング  gauss()*9ms（16分の裏はさらに +5ms 後ろ）
強弱        gauss()*14（16分の裏はさらに −8）
範囲        0拍 〜 total、はみ出しはクランプ
無効化      s.humanize===false で格子どおりに戻る
```

値の根拠は Groove MIDI Dataset（人間のドラム演奏1,138ファイル）の実測で、**その3分の1程度**に抑えてある。
会話の下に敷く用途なので、生演奏そのままのルーズさは要らない。詳細は `RESEARCH_GAP_REPORT.md`。

### 2.8 場面ごとの伴奏の音量（`ACCOMP_TRIM`）

`bgm-forge.js` の `ACCOMP_TRIM = { wonder:.72, night:.62, doubt:.68 }` が、パート1〜3
（和音・ベース・内声）だけに掛かる倍率。メロディ（0）と打楽器（4）は素通し。表にない場面は1倍。

描画ループで1回だけ `trim` を計算し、**発音の3経路すべて**（`synthNote` / `sampleNote` / `chipTone`）に渡す。
片方だけに掛けると、音色を切り替えたときにバランスが変わってしまう。

同じ場面群には「和音が息をして引いていく」フェードも掛かる（`['wonder','requiem','night','doubt']`）。
減衰のフロアは場面ごと（鎮魂 .14 / 幻想 .26 / 夜空 .10 / 疑惑 .16）。

### 2.9 「場面におまかせ」の行き先（`autoPattern`）

伴奏の既定は、これまで「場面専用の型があればそれ、なければ legacy」の二択だった。
場面に `autoPattern` を持たせると、そちらが優先される（`accompanimentFor`）。疑惑が `'wave'` を使っている。

`autoPattern` を使う場面では、`SCENES` の該当行のうち効くのは `harmony`（和音の移り変わり）と
`high`（内声の音域）だけになる。`inner` / `bass` / `pad` / `gate` / `drum` は死ぬので、コメントを残してある。

### 2.10 音源のパート割り当て（`sampleKinds`）

同梱音源を使う音色で、どのパートにどの種類のサンプルを当てるかの表。`sampleNote(...,kinds)` に渡す。

| 音色 | 0 旋律 | 1 和音 | 2 低音 | 3 内声 | 4 打楽器 |
|---|---|---|---|---|---|
| 室内楽 | ピアノ | 弦 | ピアノ | ピアノ | 打楽器 |
| 柔らかいピアノ | ピアノ | ピアノ | ピアノ | ピアノ | 打楽器 |
| フルート | フルート | 弦 | ピアノ | **ピアノ** | 打楽器 |

フルートの内声をピアノにしたのは計測の結果。内声は旋律の下に置かれるため実測で45〜55に来ており、
フルートの最低音C4（60）を最大15半音下回る。引き伸ばすと濁るので、フルートは旋律だけを吹く。

---

---

## 3. これまでの修正履歴（すべて計測つき）

### 2026-09-21 · 再生位置のバーをつかんで動かせるようにする

波形（`#meter`）は再生位置を見せるだけで、触っても何も起きなかった。長い曲の途中を確かめるには
先頭から鳴らし直すしかない。波形そのものを再生位置のつまみにした。押した場所から鳴らし直し、
押したまま横へ動かすと再生頭がついてきて、離した場所から始まる。`play(target,once,offset)` に
すでにあった `offset` をそのまま使うので、再生の仕組みは変えていない。
再生頭には上下に持ち手を描き、つまんでいるあいだは太くする（つかめる場所だと分かるように）。
2小節の試聴は波形を出さないので対象外。代わりに調整欄の試聴バーも、押すだけだったのを
つかんで動かせるようにした（`state.previewScrub` の間は 100ms の更新を止め、指の位置を優先）。

キーボードでも動かせる：`role="slider"` ＋ `tabindex="0"`、←→ で5%（Shiftで1%）、Home/Endで端、
Enter/Spaceで再生・停止。`aria-valuenow` / `aria-valuetext` は再生位置の百分率を出す。
CSSは `cursor: ew-resize` と `touch-action: none`（指で動かすあいだ画面が流れないように）。

**検証：** 既存回帰テスト 816ケース＋7,200曲 PASS（単体版の一致と `?v=` の更新も含む）。
ヘッドレスChromiumで820px・390pxの2幅それぞれ、20秒のテイクを作って再生し、
波形の20%をつかんで50%→75%へ動かしたところ `aria-valuenow` が 50 → 75 と追従、
離した0.4秒後に77（＝75%から再生が続いている）。Homeで1〜2%、そこから→で8%（5%送り＋経過分）。
JSエラー0。両幅のスクリーンショットを目視し、持ち手とフォーカス枠の表示を確認。
音そのものは変更なし。JS版更新・単体版再生成済み。公開は未実施。


### 2026-09-21 · 再生ボタンを再生／停止の切り替えに統一

変更前はメインが「▶ 再生中」になり、再クリックで先頭から再生、停止は別ボタンだった。
メインを1ボタンにまとめ、「▶ 再生」→「⏸ 停止」→「▶ 再生」と切り替える。
ユーザー指定により停止のアイコンは一時停止記号を使用。停止後の再生は従来どおり先頭から。
テイク一覧の⏸表示も維持し、試聴中を含め再生中の音をメインボタンで止められる。

**検証：** メインとテイクの表示同期を既存回帰テストに追加。816ケース＋7,200曲PASS。
ヘッドレスChromeで820px・390pxの2幅それぞれ再生→停止をクリックし、音源状態がnullになること、
ラベルとテイクアイコンの切り替え、終止あり音声の自然終了時の表示復帰を確認。JSエラー0。
テスト用の1秒無音テイクを使用し、UI操作のみを検証（音質の変更なし）。両幅のスクリーンショットを目視確認。
JS版更新・単体版再生成済み。公開は未実施。


### 2026-09-21 · 新規作曲の伴奏と和音の間隔を独立に選ぶ

「伴奏を作り直す」のほうが新規作曲より展開に幅を感じるという指摘。
変更前は伴奏3型それぞれに和音の間隔が固定され、新規作曲は各場面3組だけだった。
作り直しでは和音の間隔を保持して型を変えるため、新規作曲にない組み合わせが生まれていた。

`compose` で和音の間隔を別の決定的乱数から選ぶようにした。場面の候補と抽選の重みは維持し、
伴奏の型やほかの抽選の乱数列は変更していない。作り直しの挙動も維持。

**計測：各場面seed 1〜300（24場面・7,200曲）。** 型×和音の間隔は通常3→6組、
荘厳3→9組、クリア3→3組。疑惑も設定上3→6組だが、autoPatternの専用処理が型を使わないため、
音の種類が6つになるという意味ではない。新しいリズムの型自体を追加した変更でもない。
生成設定（テンポ・音色・ループ・メロディ有無と密度・音階・打楽器・テーマ小節数・実尺）の維持と、
全組み合わせの網羅、作り直しで和音間隔を保つことを回帰テストに追加。
既存816ケースと追加7,200曲の検証PASS。JS版更新・単体版再生成済み。
画面の表示・寸法は変更していない。音の好みは未判定なので、鳴らして確認すること。公開は未実施。


### 2026-09-20 · 「場面から選び直す」の矢印を、左へ行って上へ曲がる形にする

「単純な左矢印だが、上に戻ることが分かりづらい」。

`←` をやめ、**左へ進んでから上へ曲がり、頭が上を向く矢印**にした。押すと画面の頭へ戻る
動きと形が一致する。

**文字ではなく描いている。** それらしい文字（`↰` `⤴` など）は環境で向きも形も変わり、
絵文字として色付きで出ることもある。13pxのインラインSVG（線だけ・色は文字色を継承）にして、
どの環境でも同じ形が出るようにした。ページ内の他のアイコン（意見フォームの3種）と同じ作り。

ボタンの寸法は変えていない（820px幅で高さ48px・幅148px、行の3つは同じ高さのまま）。

単体版を再生成。816ケース＋送信テスト＋配信テストPASS。

---

### 2026-09-20 · 再生欄のボタンを整える（並び・高さ・アイコン）

「停止の左じゃなくて右側に」「停止ボタンの縦幅が再生と揃ってない」
「再生ボタンには再生の三角、停止には一時停止のボタンがあると違いがわかりやすい」。

**並び。** `再生 / 停止 / ← 場面から選び直す` の順にした（選び直すを停止の右へ）。

**高さ。** 再生だけ `padding:13px 30px`、他は `7px 11px` で背丈が違っていた。padding を
そろえると再生の存在感が落ちるので、**行のほうで `align-items:stretch`** にして、
同じ行のボタンが再生の高さに合うようにした。実測で 820px幅では3つとも **48px**。
390pxでは再生が1行目（48px）、停止と選び直すが2行目（33px・2つは同じ高さ）。

**アイコン。** 再生に `▶`、停止に `■`。**一時停止（⏸）ではなく停止（■）にしてある** ——
このボタンは `stopPlayback()` で再生を破棄し、次の再生は頭から始まるため。
⏸ だと「続きから再開できる」と読めてしまう。見た目だけ揃えたいときは差し替え可能。

**文言はJSが上書きしている場所がある**（AGENTS.md §5 の落とし穴）。再生の文字は
`再生 / 再生中 / 現在の曲を再生` の3通りを `bgm-forge.js` が入れ直すので、そちらにも
`▶` を足した。停止はHTMLの文言のまま。

`?v=` を上げ、単体版を再生成。816ケース＋送信テスト＋配信テストPASS。JSエラー0。

---

### 2026-09-20 · 「場面から選び直す」を再生欄に足す

「聴いてみるで再生したあと、場面選択に戻りたい場合に、手動でスクロールするしかなかった」。

再生欄（02 聴いて確かめる）の **停止の左** に `← 場面から選び直す` を置いた。押すと画面の頭
（場面の一覧）まで滑らかに戻り、**いま選ばれている場面のボタンに焦点**が移る。
キーボードだけでもそのまま矢印キーで選び直せる。

**再生は止めない。** 聴きながら次の場面を選べるほうが用途に合う。止めたいときは隣の停止。

実測（単体版・ヘッドレスChromium）。押す前のスクロール位置から 0 まで戻る。

| 幅 | 押す前 | 押したあと | 焦点 |
|---|---|---|---|
| 820px | 414px | 0 | ☀️ 明るい |
| 390px | 863px | 0 | ☀️ 明るい |

JSエラー0。390pxでは再生が1行目、選び直すと停止が2行目に折り返す（既存の `.transport` の折り返し）。

`bgm-forge.js` の `?v=` を上げ、単体版を再生成。816ケース＋送信テスト＋配信テストPASS。

---

### 2026-09-20 · ライセンスの画面を「題目／説明／リンク」に分け、調整の閉じ方を左へ移す

「ライセンスのモーダルが読みづらいので、使用素材の題目を記載して、その説明、リンクという風に
わかるように分けて」「ここを押すと閉じますのすぐ横に詳細があるので、詳細ボタンを押すと閉じると
勘違いしそう」。

**1. 使用素材を3段に分けた。** それまでは1素材が `<p>` 1つで、名前・作者・ライセンス・加工内容・
リンクが**ひと続きの文章**だった。素材ごとに次の形にした。

- 題目（等幅・青緑）… 例「箏 — Japan_Koto_Improv」
- 説明（本文）… 「RutgerMuller ・ CC0 1.0。13分の即興演奏から6音を切り出して同梱しています。」
- リンク（等幅・横並び）… 「Freesound 365242」「同梱時の加工内容」

素材は8件（音源／シタール／箏／笛／合唱／打楽器の統計／MP3エンコーダー／ツール本体）。
区切り線を1本ずつ入れて、どこまでが1素材か分かるようにした。画面の高さは
680×440→**680×665**（1280px幅）、342×611→**342×738**（390px幅）。読む量は同じで、行が分かれたぶん伸びている。

**ビルドの探し方を文言から id に変えた。** `build_standalone.py` は
「`<p class="note">音源: VSCO 2 Community Edition…`」という**行頭の文言**で差し替え先を
探していたので、説明を書き換えるとビルドが壊れる制約があった。いまは
`<div class="credit" id="c-vsco">` の **id で探す**。単体版の同梱クレジットも同じ3段の形にそろえた。
**この制約が消えたので、AGENTS.md の「クレジット行の行頭を書き換えるな」も書き換えた。**

**2. 調整の閉じ方を「調整中」の隣へ移した。** 右端に置いていたため、すぐ横の `詳細` ボタンが
閉じるボタンに見えるという指摘。文言も **「この赤枠を押すと閉じます」** にして、枠と同じ赤系の色にした。

狭い画面では、左へ寄せても折り返して `詳細` と同じ行に並ぶ。**560px以下では閉じ方を1行専有**に
した（`#stepAdjust>.closehint{flex:none}` のほうが詳しい指定なので、id を含めないと効かない）。
実測で、390pxでは閉じ方と `詳細` が**別の行**（見出しの高さ 80→108px）、820pxでは今まで通り1行（46px）。

単体版を再生成。816ケース＋送信テスト＋配信テストPASS。JSエラー0。

---

### 2026-09-20 · 手元の整形を取り込み、ライセンス欄に見出しを付ける

「bgm_forge.html 一部モーダルを手動調整しました、単一版にも反映を」
「ライセンス欄は突然始まるので題目等があったほうがいいでしょうか」。

**取り込んだもの。** `bgm_forge.html` がエディタの自動整形を通っていて、差分は
**2,787行増・715行減**＝ファイルほぼ全体だった。空白を全部落として比べると、
**中身の変化は1か所だけ**。テイクの詳細（`d-save`）から「作った曲の扱い」の見出しと本文を
削除したもの。権利の話はライセンス欄にまとまったので、二重に置かない形になった。

整形は表示に影響していない（単体版で実測）。

| 見たもの | 整形前 | 整形後 |
|---|---|---|
| ライセンスの詳細ボタンの右端 | main右端−19px | 同じ |
| ライセンスの画面 | 680×440／342×611 | 同じ |
| 調整の見出しの高さ | 46px／80px | 同じ |
| 既定の場面 | ☀️ 明るい | 同じ |
| 横のはみ出し・JSエラー | なし・0 | なし・0 |

**テストが1件落ちた。** GitHub Pages から新URLへ飛ばす1行を文字列一致で見ていたため、
整形で空白が入っただけで落ちた（`if (location.hostname === ...`）。**テスト側を空白に鈍く**した
（空白を落としてから比較）。同じことが起きるので、AGENTS.md §5 にも書いた。

**見出しを付けた。** 権利の2行がテイクの枠のすぐ下から前触れなく始まっていた。
小さな見出し **「権利とライセンス」**（他の節の見出しと同じ等幅・字間広め・淡色）を置き、
区切り線は見出しが持つようにした。2行は線を引かずに続ける。
詳細ボタンの位置は変えていない（1行目の右端）。

単体版を再生成。816ケース＋送信テスト＋配信テストPASS。

---

### 2026-09-20 · 既定の場面を「明るい」に、調整欄に閉じ方を出す、ライセンスの詳細を1行目へ

4件まとめて。音には触っていない。

**1. 既定の場面を「明るい」にした。** 既定が 🍺街・酒場 だった（`state.mood` が `id==='town'`）。
場面を6×4に並べ替えたあとも既定だけ左上ではなかったので、`id==='bright'` にした。
**idで指しているので並べ替えても壊れない**（AGENTS.md の落とし穴）。テンポと音色は
`selectMood` が起動時に `DEFAULTS` から入れ直すので、状態の初期値は触らなくてよい。
起動直後の表示は「☀️ 明るい／木のマレットが軽く跳ねます」。

**2. 調整欄の閉じ方を出した。** 「この曲を調整」は `details` なので見出しを押せば閉じるが、
それが分かりにくいという指摘。開いているあいだだけ、見出しの右に
**「ここを押すと閉じます」** を出し、**調整欄の一番下に「✗ 調整を閉じる」** を足した。
下のボタンから閉じたときは見出しが画面外へ行くので、`scrollIntoView` で戻している
（実測: 閉じたあと見出しが画面内にある＝true、820px／390pxとも）。

見出しは `display:flex` で折り返し無しだったため、文言を1つ足しただけで390pxで
「この曲を／調整」「調整／中」と割れた。`flex-wrap:wrap` と、バッジ・ヒントに `flex:none` を
入れて、狭いときは**行ごと折り返す**ようにした（390pxで見出しの高さ 46→80px、文字は割れない）。

**3. ライセンスの「詳細」を1行目へ移した。** 「このツールで作った曲は自由に使えます」の行に置いた。
右端の位置は前回と同じ（`main` の右端から19px内側、他の節の詳細と一致）。

**4. `favicon.png` をコミットした。** Discordのアイコン用に作ったもの。必須ではないが、
リポジトリの外に置くと行方が分からなくなるので一緒に置いた。公開物には含まれない
（`build_hosting.py` の配布一覧に入っていないため）。

`bgm-forge.js` の `?v=` を上げ、単体版を再生成。816ケース＋送信テスト＋配信テストPASS。JSエラー0。

---

### 2026-09-20 · ライセンスの「詳細」を他の節と同じ位置にそろえ、GitHubへのリンクを足す

「他の詳細ボタンの位置と違いすぎるので同じ位置になるよう調整してほしい」
「Code自体は MIT License の横に、GITHUBのリンクもつけてほしい」。

**位置。** 各節の `詳細` は `.panel h2` の中で `margin-left:auto` により右端に出る。`.panel` は
枠1px＋左右余白18pxなので、**`main` の右端より19px内側**に並ぶ。ライセンスの行は `main` の直下に
あって文章の後ろに続いていたため、この列から外れていた。行を横並び（flex）にして、ボタンに
`margin-left:auto` と `margin-right:19px` を与えた。

実測（ヘッドレスChromium）。`main` の右端からの距離。

| 幅 | 聴いて確かめる | テイク | ライセンス |
|---|---|---|---|
| 1280px | 19px | 19px | **19px** |
| 820px | 19px | 19px | **19px** |
| 390px | 19px | 19px | **19px** |

390pxでは文章がボタンの左で折り返す。テイクの節と同じ見え方になる。

**GitHub。** `ツール本体のコードは MIT License（Copyright © 2026 air-nakamoto）。` の
MIT License の隣に `· GitHub`（https://github.com/air-nakamoto/bgm-forge）を足した。

ついでに、**単体版のMITリンクが開けないままだったのを直した**。単体版は1ファイルで配るので
隣に `LICENSE` が無く、`<a href="LICENSE">` は何も開かない。MIT本文は先頭のコメントに同梱して
あるが、リンク先はGitHubの原本（`blob/main/LICENSE`）へ向けるようにした（`build_standalone.py`）。
分割版は同じフォルダの `LICENSE` のままでよい。テストで両方を固定した。

単体版を再生成。816ケース＋送信テスト＋配信テストPASS。

---

### 2026-09-20 · 使用素材・ライセンスを、他の詳細と同じ重ねて出す画面にする

「作った曲の扱いって、ライセンス欄に書けばいい気がしている」「これをほかの詳細と同じ感じの
ボタンとモーダル表示にしたい」。

権利の2行はページに出したまま。その下にあった **折りたたみ（`details.material-credits`）を
やめ**、2行目の末尾に他の節と同じ `詳細` ボタンを置いて、`#license` の重ねて出す画面にした。
見た目は「使い方」「テイク」などの詳細と同じ（`card help` ＋ `✗ 閉じる`）。**文面は1文字も変えていない。**

| | 前 | 後 |
|---|---|---|
| 入口 | 折りたたみ「使用素材・ライセンス」 | 権利2行目の末尾の `詳細` ボタン |
| 出方 | その場で下に開く | 重ねて出す画面（Escape・背景クリック・✗ 閉じるで閉じる） |

**中身は複製せずに出している。** 他の詳細（`data-more`）は `openDetail` が `innerHTML` を
`#detailBody` へ複製する作りだが、単体版のこの画面はCC0とLGPLの全文・エンコーダー原本（base64）で
**4.1MB** ある。複製すると開くたびにその分の文字列を作ることになるので、`#license` は
HTMLに置いたまま `hidden` を外すだけにした。

実測（単体版・ヘッドレスChromium）。**開くまで 1280px幅で157ms・390px幅で96ms、JSエラー0。**
Escape・背景クリックの両方で閉じ、開いた直後の焦点は「✗ 閉じる」。
画面の大きさは 680×440（1280px幅）／342×611（390px幅）ではみ出しなし。

単体版では、差し替え後の同梱クレジット（CC0全文・LGPL全文・原本の保存リンク）が
そのままこの画面の中に入る。`build_standalone.py` の3つの正規表現は行頭の文言で探しているので、
場所が変わっても当たる（**行頭の文言は引き続き書き換えないこと**）。

`bgm-forge.js` の `?v=` を上げ、単体版を再生成。816ケース＋送信テスト＋配信テストPASS。
折りたたみへ戻さないためのアサートをテストに追加した。

---

### 2026-09-20 · 「閉じる」を3か所とも同じボタンにする

「閉じるのボタンデザインを共通させたい。✗閉じる　がいいな」。

重ねて出す画面は3つあり、閉じ方の見た目がばらばらだった。

| 画面 | 前 | 後 |
|---|---|---|
| ? 使い方 | `閉じる`（枠つき・11pxの等幅） | **`✗ 閉じる`** |
| 場面などの詳細 | `閉じる`（同上） | **`✗ 閉じる`** |
| 意見・要望を送る | `×` だけ（枠なし・26px角の四角） | **`✗ 閉じる`**（同じ枠つきボタン） |

意見フォームの閉じるは `.helpclose` を付けて見た目を他と同じにし、CSSは位置（右上）だけ指定に減らした。
見出しがボタンに当たらないよう、右の逃げを32→92pxに広げた。

実測（ヘッドレスChromium）。3か所とも **79×25px・11pxの等幅・文言も同じ**。
見出し右端とボタン左端のすきまは 1280px幅で17px、390px幅で13px。重なりなし。
820px／390pxで3画面とも目視確認済み。

単体版を再生成（使い方と詳細の2か所は単体版にも入る）。816ケース＋送信テスト＋配信テストPASS。

---

### 2026-09-20 · 意見フォームが大きすぎたので本体の寸法に合わせる

「意見を送るをクリックしたあとに表示される画面サイズがおおきい」。

**測ってから直した。** 1280×800 で開くと、フォームは **640×711＝画面の高さの89%** を占めていた。
中の文字も本体より一回り大きい（フォーム15px／本文13px、見出し24px、送信ボタン17px太字で高さ56px）。
本体の「使い方」シートは14.5pxなので、**このフォームだけが別のサイトの部品に見えていた**のが正体。

寸法を本体に合わせた。機能・項目・文言は1つも変えていない。

| 場所 | 前 | 後 |
|---|---|---|
| 幅 | 640px | 440px |
| 本文の文字 | 15px | 13px（本体と同じ） |
| 見出し | 24px | 16px |
| 種類のタイル | 上下16px・アイコン26px・126px高 | 上下9px・アイコン18px・89px高 |
| 入力欄 | 154px高（15px） | 130px高（13px・行数は5のまま） |
| 送信ボタン | 17px太字・56px高 | 14px太字・41px高 |
| 余白（頭／本体） | 24/28px | 14/18px・16/18px |

| 画面 | 前 | 後 |
|---|---|---|
| 1280×800 | 640×711（高さの89%） | **440×543（68%）** |
| 390×844 | 354×706（84%） | **354×550（65%）** |
| 1280×640・返信希望オン | 604まで縮んで107はみ出し | 604・はみ出し47 |

横のはみ出しは3幅とも0。返信希望をオンにして連絡先欄が出た状態も確認済み
（390×844 で 354×658、はみ出し0）。ヘッドレスChromiumで実測。

バージョン表示は 2026.09.20.3 に上げた。単体版を再生成（送信機能は入らないので痕跡0のまま）、
816ケース＋送信テスト＋配信テストPASS。**鳴らす音には触っていない。**

---

### 2026-09-20 · 本体をCloudflare Workers Static Assetsへ移行

公開URLを https://bgm-forge.suihei.workers.dev/ に変更。`hosting/wrangler.toml` と
`scripts/build_hosting.py` を追加し、公開用に必要なHTML・JS・同梱素材・ライセンスだけを抽出。
最大ファイル20,833,251 bytes（単体版）、Cloudflareの25MiB上限内。
原音・秘密・Workerソース・未追跡favicon.pngは公開対象外。公開上の /.git/config と /worker/feedback.js は404。
旧ファイル名2つは302でルートへ。GitHub Pages上の分割版は新ドメインへ移動（検索文字列とハッシュ保持）。
単体版からはこの転送も除去するので、オフライン配布の振る舞いは維持。
OG URLと画像URLを移行先へ更新。中継のOrigin許可に新ドメインを追加。
新Originから空本文で400 empty、Access-Control-Allow-Originが新ドメインと一致。Discordへ実投稿はしていない。
公開コマンドはビルド・テスト→中継更新→本体更新→GitHub保存。未pushがなくても再デプロイできる。
配信対象・旧URL・CORS・単体版の隔離をtests/hosting.cjsで回帰テストに固定（816ケースもPASS）。
初回のhtml_handling=noneではルートが404になるため、auto-trailing-slashへ修正して再公開。
公開サイトの820px／390pxで24場面・意見フォームを確認、JSエラー0。820pxで実際に1曲生成しWAVダウンロード成功。画面も目視確認済み。


### 2026-09-20 · 意見フォームをページの配色に合わせ、入口を右上へ移動

白いフォームを本体と同じ暗い背景・青緑の線・小さい角丸へ変更。投稿種類と個別回答の機能は維持。
ヘルプに隣接していた意見ボタンをheader直下の右上へ移し、入口の誤操作を避ける。
820px／390pxのヘッドレスChromeで目視確認。ボタン上端は両幅32px、右端は806px／376px（画面右余白14px）。
フォーム幅640px／354px、横はみ出し0。ヘルプの隣へ戻さない構造アサートを既存テストに追加。
バージョン2026.09.20.2、JS読込版更新・単体版再生成、816ケースと送信テストPASS。


### 2026-09-20 · 参考フォームに合わせて投稿分類とDiscordカードを追加

APNG Generatorの参考画像に合わせ、白いダイアログ・3種類の選択・任意の個別回答希望欄を実装。
分類は0→3種類（不具合／要望／感想）、Discordは本文だけ→色付きembed 1枚に変更。
本文2000文字、連絡先200文字。返信希望時だけ連絡先を必須として送り、希望しない場合は送らない。
バージョン2026.09.20.1とUser Agentも送信し、画面で明示。曲・設定・指紋は送らない。
Workerは旧フォームの本文だけの投稿にも対応。wait=trueでDiscord保存確認を待つ。
模擬送信で3分類、連絡先、メンション抑止、旧形式、入力検査、honeypot、Origin、連投制限を検証。
`tests/feedback.cjs`を`tests/scene-variation.cjs`から実行して固定。816ケースPASS。
ヘッドレスChromeで820px／390px、返信希望オン／オフの4画面を確認。カード幅640px／354px、横はみ出し0。
390pxの初回は固定幅のカードが切れていたためgridの最小幅を0にして修正。
ブラウザ模擬送信は2幅とも成功、返信希望で連絡先空の場合は送信0件。
単体版を再生成し送信機能の痕跡0を維持。実Discordへのテスト投稿はしていない。


### 2026-09-20 · 公開URLからv2を外す

正本を `bgm_forge.html` へ改名。旧 `bgm_forge_v2.html` は新URLへ自動転送する小さいページとし、
クエリとハッシュを引き継ぐ。ページタイトルのV2、OG URL、公開コマンドの案内、ビルドとテストの参照も更新。
この文書内の参照は現行ファイル名へ統一した。ライセンス条件は変更せず対象ファイル名のみ更新。
ヘッドレスChromeで820px／390pxの2幅とも旧URLから転送、クエリ・ハッシュ保持、横はみ出し0を確認し画像を目視。
旧URLの転送先を回帰アサートに固定。単体版再生成後816ケースPASS。
Workerへ空JSONを送った応答は503 not-configuredから400 emptyへ変化し、Webhook登録済みを確認。
Discordへの実投稿はしていない。未追跡の `favicon.png` は今回の変更に含めない。


### 2026-09-20 · 意見の送信先を公開済みWorkerへ設定

利用者が公開した `https://bgm-forge-feedback.suihei.workers.dev` を分割版のmetaへ設定。
JSの版を更新し、単体版を再生成。816ケースPASS、単体版の送信機能除去も確認。
ヘッドレスChromeで820px／390pxの2幅ともボタン表示・ダイアログ開閉入口・横はみ出しなしを確認し、スクリーンショットを目視した。
Workerへ空のJSON（Discordへの投稿には至らない）を送り、HTTP 503 / `not-configured` を確認。
Webhookは未登録。次は利用者が `npx wrangler secret put DISCORD_WEBHOOK` で登録し、ページを公開する。
実メッセージの送信・到達確認は未実施。


### 2026-09-20 · 「意見を送る」を足す（Cloudflare Worker 経由でDiscordへ）

「https://apng-generator-tan.vercel.app/ これみたいに意見を送るを実装したい」「元々Discordで
送られるようにしているので同じようにしたい」「単体版に送信機能は不要だよね」。曲の指紋は**添えない**と
決めた（「添えない」）。

**Webhook URLをページに置くと誰でも叩ける**ので、あいだに Cloudflare Worker を挟んだ。
ページは Worker のURLだけを知り、Discord の Webhook URL は Worker の秘密（`wrangler secret`）に入る。

| 置き場 | 持つもの |
|---|---|
| `bgm_forge.html` の `<meta name="feedback-endpoint">` | Worker のURL（公開してよい） |
| Worker の環境変数 `DISCORD_WEBHOOK` | Webhook URL（リポジトリにもチャットにも出さない） |
| Worker の変数 `ALLOWED_ORIGINS` | 受け付ける出どころ。既定は GitHub Pages のドメインのみ |

Worker 側（`worker/feedback.js`）で止めているもの。

- **出どころ違い** — `Origin` が `ALLOWED_ORIGINS` に無ければ403。CORSヘッダも返さない。
- **連投** — Cache API に送信元IPを60秒置く。ある間は429（画面は「少し待ってから」と出す）。
- **機械** — 画面に出ない入力欄（honeypot）が埋まっていたら、成功を返して捨てる。
- **メンション爆撃** — `@everyone` `@here` `<@123>` にゼロ幅空白を挟み、`allowed_mentions:{parse:[]}` も付ける。
- **長文** — 2000文字で切る（Discordの上限）。画面側も `maxlength` と残り字数で同じ上限。

**当初は送るのは本文だけ（同日後続のフォーム改修で分類・動作環境・任意の連絡先を追加）。** 曲も設定も指紋も送らない。名前も連絡先も取らないので返信はできない、と
ダイアログに明記した。

**単体起動版には入れない。** オフラインで配る版から外へ出る通信をなくすため、`feedback:start`〜
`feedback:end` で囲った6か所（meta・ヘッダのボタン・下の案内文・ダイアログ・CSS・配線のJS）を
`scripts/build_standalone.py` が丸ごと落とす。落としたあと `"feedback" not in doc` をアサートしている。
配線のJSを `bgm-forge.js` ではなくHTMLに直接置いたのは、**単体版に痕跡を1文字も残さないため**
（`bgm-forge.js` は単体版へ1バイトも違わず埋め込まれる決まりなので、あちらに書くと死んだコードが残る）。

計測と確認（ヘッドレスChromium、820px／390px）。

| 見たこと | 結果 |
|---|---|
| 送信先が空（既定・ローカルで開いたとき） | ヘッダのボタン・下の案内文・ダイアログとも `hidden` のまま。JSは即return |
| 送信先を入れたとき | 両方のボタンが出る。押すとダイアログ、字数が `20 / 2000` と動く |
| 届かない相手へ送ったとき | 「送れませんでした。通信を確認してください。」ボタンは押せる状態に戻る（固まらない） |
| 390px | ヘッダのボタン2つが1行に収まる。ダイアログも折り返し事故なし |
| 単体版 | `feedback` の文字列が0件 |

テストに固定した（`tests/scene-variation.cjs`）。分割版に `<meta name="feedback-endpoint">`・
`data-feedback-open`・`id="feedbackText"` があること、単体版にその4語が**無い**こと。

**まだ動いていない。** Worker のデプロイと `DISCORD_WEBHOOK` の設定、`<meta>` へのURL記入は
アイアールさんの作業（手順は §6.0）。それまでは送信先が空なので、画面に「意見を送る」は出ない。

---

### 2026-09-20 · クレジットに出典リンクを足す（笛が丸ごと抜けていた）

「ライセンス系に提供もとリンク情報もいれたい」。

**見に行って、笛のクレジットが1行も無いことに気づいた。** 同日に実録音へ差し替えたのに、
画面の「使用素材・ライセンス」に載せ忘れていた。**同梱音源がCC0であることが、このツールの
生成物をCC0と言い切れる根拠**（AGENTS.md §6）なので、載っていないのは表示の穴。追加した。

出典リンクを全項目に付けた。

| 項目 | 追加したリンク |
|---|---|
| Groove MIDI Dataset | データセット · CC BY 4.0 |
| VSCO 2 CE | 原典（GitHub）· 公式 · 同梱の音源情報 |
| シタール | Freesound 42192 · 音源情報 |
| 箏 | Freesound 365242 · 音源情報 |
| **笛（新規）** | **Freesound 553217 · 音源情報** |
| 合唱 | Freesound 764124 · 音源情報 |
| MP3エンコーダー | ソース · LAME · lamejs · LGPL 2.1 |
| ツール本体 | MIT License（同梱のLICENSE） |

**権利の2行は、折りたたみの外に出した**（同日に指摘を受けて移動）。開かなくても読める位置に、
続けて2行。

> このツールで作った曲は自由に使えます（CC0 1.0 相当・クレジット不要・商用可）。
> 同梱音源はすべて CC0 1.0 です。その為このツールで作った曲に素材由来の条件は付きません。

1行目は元から外にあった。2行目（CC0全文へのリンク付き）を中から出して並べた。
`main>.note` は上に区切り線を引く指定なので、2行続けると線が二重になる。
`main>.note+.note` で2行目の線と余白を消してある。単体起動版は分割版から組み立てるので、
外に出した行はそのまま引き継がれる（差し替え対象の正規表現には掛からない）。

**単体起動版にも同じ情報を入れた**（`scripts/build_standalone.py`）。実録音4種をFreesoundの
リンク付きで並べ、Groove MIDI にもリンクを付けた。加工の内容は従来どおり「音源の原典・
加工内容」の折りたたみに全文が入る。

**行頭の文言は変えていない。** `build_standalone.py` が `NOTE_SOURCE` / `NOTE_LAME` /
`NOTE_GROOVE` の3つの正規表現で行を探して差し替えているので、リンクは**行の末尾に足した**。
行頭を書き換えると単体版の組み立てが静かに壊れる（`NOTE_GROOVE` はアサート無しで消すだけ）。

ヘッドレスChromiumで820pxと390pxの2幅、折りたたみを開いた状態で確認済み。816ケースPASS。

### 2026-09-20 · 一番下に「ページの一番上に戻る」を足す

「ページ一番したに、一番上に戻れるリンクがほしい」。場面が24個に増えてページが縦に長く
なったため（820px幅で1589px、390px幅で**2256px**）、読み終わりから頭へ戻る手段を置いた。

`<main>` に `id="top"` を付け、クレジットの折りたたみの後ろに
`<p class="backtop"><a href="#top">↑ ページの一番上に戻る</a></p>`。
見た目は既存の `main>.note` と同じ区切り線を上に引いて中央寄せ。JSは足していない。

ヘッドレスChromiumで820pxと390pxの2幅を撮って確認済み（AGENTS.md §1）。

### 2026-09-20 · 場面の説明文を2か所直す（「儀式」の重なりと、決断の性格）

**神楽から「儀式」を外した。** 「神楽にも儀式と描かれて折り曲がらわしいので神楽の説明から
儀式をはずしたい」。🕯️儀式という場面が別にあるのに、⛩️神楽の説明にも「和風の儀式」と
書いてあって、選ぶときに迷う。「巫女の舞」に変えた。

神楽と儀式は**和音進行・旋法・打楽器が同一の双子**で、違うのは音色・主音・エネルギーだけ。
区別を付けているのは説明文なので、この2つの文面をいじるときは近さを意識すること。
**同じ重なりが鎮魂の「儀式としての弔い」にも残っている**（§6.0 の 4 番）。

**決断に「シリアス」を入れた。** 「決断の説明にシリアスといれたい」。
テンポも和音も触っていない。文面だけ。

| 場面 | 前 | 後 |
|---|---|---|
| ⛩️ 神楽 | 「…封印、神降ろし、**和風の儀式**。」 | 「…封印、神降ろし、**巫女の舞**。」 |
| ⚖️ 決断 | 「重大な選択、対立、真相を前にした沈黙。」 | 同左＋「**シリアスな空気にしたい場面**や、引き返せない決断を迫られる場面に。」 |

### 2026-09-20 · オルゴールをオルゴールの音にする（整数倍音で積んでいた）

「オルゴールもオルゴールっぽい音にさしかえたいな」。

**原因は分音の比。** オルゴールは回る筒の突起が金属の櫛歯を撥く楽器で、鳴っているのは
**棒の振動**。棒の固有振動は整数倍にならない。ところが実装は整数倍で積んでいた。

| 音色 | 分音 | いちばん大きい分音から見た比 |
|---|---|---|
| **オルゴール（前）** | 4 / 8 / 12 / 2 | **1 : 2.00 : 3.00 : 0.50** ← 弦や管と同じ整数倍 |
| **オルゴール（後）** | 4 / 11.04 / 21.6 / 2 | **1 : 2.76 : 5.40 : 0.50** |
| ガラス・鐘（元から） | 1 / 2.76 / 5.4 / 8.9 | 1 : 2.76 : 5.40 : 8.90 |
| 参考・棒（櫛歯・鉄琴） | — | 1 : 2.76 : 5.40 : 8.90 |
| 参考・弦や管 | — | 1 : 2.00 : 3.00 : 4.00 |

**同じファイルの「ガラス・鐘」のほうが、よほど棒らしい比を使っていた。** オルゴールは
澄んだ正弦の鐘にはなっても、金属を撥いた音にはなりようがなかった。

**鳴る音域は変えていない。** いちばん大きい分音は基音の4倍（2オクターブ上）のまま。
夜空の音の高さは動かず、その上に棒の第2・第3モードが乗る形。

併せて撥弦らしさを出すため、立ち上がりを 2.0→1.5ミリ秒、余韻を 0.5→0.9秒、
減衰の落とし先を .02→.07（長く残る）、ローパスを `[20,9]/2600`→`[24,11]/3200`（明るく）。

**高い分音が速く減るのは元からの仕組み。** `synthNote` は減衰の落とし先を `decay/ratio` に
しているので、比の大きい分音ほど深く落ちる。撥いた瞬間だけ金属が光って、あとは基音が長く
残る＝櫛歯の鳴り方になる。今回はその仕組みに正しい比を与えただけ。

816ケースPASS。**未確認：聴いてオルゴールに聞こえるか。** 摘みは分音の比（2.76 / 5.4）、
余韻（`r:.9`）、減衰の落とし先（`decay:.07`）、明るさ（`filt`/`floor`）。
実録音に差し替える道もあるが、棒の音は合成が効きやすいので、まず合成で詰めるのが安い。

### 2026-09-20 · 場面を24に増やし、並びを4行×6のグループにする

「この並び方に規則性ほしいね」「可能ならひとつ場面増やしたい」。**23は6で割り切れず**、
画面が6列なので最終行だけ5個で余っていた。1つ足して24にすると、6列×4行ちょうどになる。

**並びの規則：行が場面のまとまり、左から右が物語の順。**
（最初は行内を明るい→暗い順にしたが、同じ日に「左から物語の序盤、右にいくにつれ終わり」と
指定されたので入れ替えた。例として挙がったのが1行目の 明るい→街・酒場→のどか→カジノ→クリア。）

**最終的な並びは本人が指定したもの**（2026-09-20）。1行目と3行目は、こちらが当てた順から
本人の手で入れ替えられている。**並べ替えるときは本人に確認すること。**

| 行 | 場面（左が序盤、右が終盤） | 物語の流れ |
|---|---|---|
| 日常・明るい | ☀️明るい 🌾のどか **🌊水辺** 🍺街・酒場 🎲カジノ 🎉クリア | 導入→野を行く→水を渡る→街に着く→山場→解決 |
| 旅・異郷・神秘 | 🌍民族 🎐和風 🌌夜空 ✨幻想 ⛪荘厳 ⛩️神楽 | 異郷に着く→その土地→夜を越す→不思議に触れる→厳かな場→神降ろし |
| 情緒・内面 | 🔍疑惑 🧩思索 📻回想 💧悲哀 🕊️鎮魂 ⚖️決断 | 疑う→調べる→思い出す→失う→弔う→決める |
| 緊張・暗黒 | 🌙暗い ⚙️機械 🏃追跡 🩸恐怖 🕯️儀式 🔥緊迫 | 不穏→無機質な奥へ→逃走→逃げ場がない→儀式の只中→決着 |

こちらが当てた順との違いは2つ。**1行目は街より先に自然（のどか・水辺）が来る** ―
街に「着く」形になった。**3行目は決断が最後** ― 弔いまで見届けてから決める流れ。

前は のどか・荘厳 が2行目、儀式 が4行目にあり、明るさ順にも用途順にもなっていなかった。

**足したのは 🌊 水辺。** 港、渡し船、川べり、水辺の集落。**水系の場面が1つも無かった**のが理由。
1行目の末尾（のどかの次）に置いた。テンポ76・柔らかいピアノ・リディア旋法・水滴に見立てた
ticks の打楽器で、**1行目でいちばん静かな場面**になる。

| 1行目の6場面 | テンポ | 音色 | 旋法 | 打楽器 | 音符 |
|---|---|---|---|---|---|
| 明るい | 96 | wood | ionian | 48発/分 | 4.58個/秒 |
| 街・酒場 | 76 | pluck | mixolydian | 95発/分 | 3.85個/秒 |
| カジノ | 116 | samples | ionian | 145発/分 | 7.97個/秒 |
| クリア | 116 | wood | ionian | 58発/分 | 7.52個/秒 |
| のどか | 96 | steel | mixolydian | 120発/分 | 5.32個/秒 |
| **水辺** | **76** | **softpiano** | **lydian** | **57発/分** | **3.56個/秒** |

`softpiano`（柔らかいピアノ）は**どの場面の既定にも使われていなかった**音色なので、これに充てた。

**踏みかけた落とし穴を2つ。**

- **既定の場面が添字だった**（`state.mood=MOODS[1]`）。並べ替えると既定が変わる。
  `MOODS.find(m=>m.id==='town')` に直した。たまたま新しい並びでも添字1が街・酒場だったが、
  次に並べ替えたら壊れるところだった。
- **場面ごとの伴奏の形は全部違っていなければならない**（`scene rhythm collision` のテスト）。
  `SCENES.water` の `inner`/`bass`/`pad`/`hold`/`harmony` は既存23と重ならない組み合わせにした。
  テストの表示が「24 distinct scene rhythms」に増えているのが確認になる（816ケースPASS）。

**MOODS の先頭に、行の割り当てをコメントで書いた。** 場面を足すときは、どの行に入るかを決めてから。
6の倍数を崩すと行の意味が見えなくなる。

**未確認：聴いて水辺が水辺らしいか。** 摘みはテンポ・音色（`softpiano`）・旋法（`lydian`）・
打楽器（`ticks`）・`SCENES.water` の伴奏の形。

### 2026-09-20 · シタールの「じゃらん」を外す（4回直しても不自然が取れなかった）

「やっぱり2回なるじゃらんは不自然でしかないな。元々の目的はシタールをじゃらんと弾いた音が
入ったら異国情緒でるかなが目的だった」。**撤去した。**

**4回直して、毎回べつの欠陥が出た。** 記録として残す。

| 回 | 指摘 | 原因（計測） | 直し方 |
|---|---|---|---|
| 1 | 「変な重なりかたでうるさい」 | 同時に鳴る音の間隔の**92%が2半音以下**（音階順に5音） | 和音構成音だけを1オクターブ以上に散らす |
| 2 | 「不自然」「あともコードが続く」 | じゃらん直後3秒に**延べ11.67秒**鳴っていた（4音を2秒押さえっぱなし） | 音符を2.6拍→0.4拍、3音、不等間隔に |
| 3 | 「ぱらぱら感がない」 | 発音間隔35ミリ秒が詰まりすぎ（実物は60〜150ミリ秒） | 0/80/160ミリ秒に（別セッション） |
| 4 | 「2回なるじゃらんは不自然でしかない」 | **仕組みそのもの** | 撤去 |

**残った不自然さは、音の作りではなく置き方だった。** 小節0と小節4にきっかり、毎回まったく
同じ形で現れる6音は、奏者の仕草ではなく機械の目印に聞こえる。音程・間隔・長さ・音量を
どう調整しても、この規則性は消せなかった。

**目的（異国情緒）は、じゃらん抜きで足りている。** いま民族が持っているのは、
ヒジャーズ旋法（増2度）・持続低音・実録音シタールのアルペジオ・遠い太鼓。

| 民族・1曲あたりの音符 | じゃらん有り | **じゃらん撤去後** |
|---|---|---|
| 和音（合成） | 24.5音 | 24.5音 |
| 低音（合成） | 16.9音 | 16.9音 |
| **シタール（実録音）** | 17.0音（うち6音がじゃらん） | **11.0音**（内声のみ） |
| 打楽器 | 4.0音 | 4.0音 |

**未確認：聴いてシタールの量が足りているか。** 11.0音は、じゃらんを抜いた内声だけの量で、
この状態はまだ耳で判定されていない。薄ければ内声を奇数小節から増やす
（毎小節なら24.1音）。表は §3「民族の内声を奇数小節だけ鳴らす」にある。

### 2026-09-20 · 民族の内声を奇数小節だけ鳴らす（止めるか毎小節か、の中間を取る）

「じゃラーンの音の不自然さは消えたけど、あんまり民族っぽくなくなった」。`3ec20ce` で内声を
丸ごと止めた副作用（§3 の一つ下）がそのまま出た。民族は旋律オフなので、内声を止めると
実録音シタールが鳴るのは**じゃらんの6音だけ**になる。

**5通りを同じ物差しで測って決めた。**

| 内声の鳴らし方 | シタール | 全体に占める割合 | 音符 | じゃらん直後3秒の内声 |
|---|---|---|---|---|
| 止める（`3ec20ce`） | 6.0音/曲 | 12% | 2.04個/秒 | 0.00秒 |
| 毎小節（止める前） | 30.1音/曲 | 40% | 2.99個/秒 | **4.08秒** |
| **奇数小節（採用）** | **17.0音/曲** | **27%** | **2.47個/秒** | **0.00秒** |
| 4小節の後半（2,3小節目） | 17.0音/曲 | 27% | 2.47個/秒 | 0.00秒 |
| 4小節に1回（3小節目） | 10.5音/曲 | 19% | 2.22個/秒 | 0.00秒 |

音数が同じ17.0音でも、**奇数小節のほうを採った**理由は並び方。奇数小節なら
**アルペジオの小節が2つ続くことが無く**（必ず1小節空く）、**じゃらんのある小節（4の倍数）は
必ず空く**。「4小節の後半」は2小節連続になるので、局所的に「続く」が戻りかねない。

じゃらん直後3秒の内声は **0.00秒のまま**なので、「コードがたくさん続く」は再発しない。

**テストを書き換えた。** 別セッションが入れた「民族の内声は0音が正常」というアサートは、
今回の方針と真逆なので置き換えた。いまは (a) 内声が鳴ること (b) 奇数小節だけであること
(c) じゃらんの小節に入らないこと (d) アルペジオの小節が2つ続かないこと、の4つを固定している。

**未確認：聴いて民族らしさが戻ったか。** 足りなければ毎小節寄りに（`bar%2===1` を外す方向）、
多すぎれば `bar%4===3`（10.5音）に。数字は上の表にある。

### 2026-09-20 · 民族の内声を止め、じゃらんの間隔を実物に合わせる（別セッションの作業・計測は後追い）

コミット `3ec20ce`。**別のセッションが行った変更**で、§3 への記録が無かったので後から測って足した。
変更そのものには手を入れていない。指摘は「ぱらぱら感がない」「アルペジオが続く」。

**変えたのは2つ。**

1. `bgm-score.js` の内声アルペジオを民族では鳴らさない（`if(!breath&&s.moodId!=='ethnic')`）
2. じゃらんの発音間隔を **0 / 35 / 27ミリ秒 → 0 / 80 / 160ミリ秒**（`at=[0,.08,.16]`）。
   実際のシタールの撥弦間隔は60〜150ミリ秒なので、35ミリ秒は詰まりすぎて同時打に聞こえていた

**テストの上限も直っている**（`gap<0.08` → `gap<0.20`）。0.08拍がちょうど境界にぶつかって
落ちていた。上限のほうが実物に合っていなかったので、緩めたのが正しい。

**計測（民族100曲・既定設定）**

| じゃらん直後3秒の延べ秒数 | 2回前 | 前 | 今 |
|---|---|---|---|
| じゃらん | 11.67秒 | 4.55秒 | 4.55秒 |
| 和音（パッド） | 10.06秒 | 10.06秒 | 10.06秒 |
| **内声** | **4.08秒** | **4.08秒** | **0秒**（停止） |

**副作用：民族からシタールがほとんど消えた。** 民族は旋律オフなので、内声を止めると
実録音シタールが鳴るのは**じゃらんだけ**になる。

| 民族・1曲あたりの音符 | |
|---|---|
| 和音（合成） | 24.5音 |
| 低音（合成） | 16.9音 |
| **シタール（実録音）** | **6.0音**（3音×2回のじゃらんのみ） |
| 打楽器 | 4.0音 |

説明文は「民族弦の乾いた撥弦と、増2度を含む東方の音階」のままなので、**文面と実体がずれている**。
§6.0 に残課題として書いた。

### 2026-09-20 · 民族の調が動くのを止め、シタールの「ジャラーン」を足す

「民族で途中で調が変わるの微妙かも」「シタール特有の弦楽器のジャラララーんってかんじがない」。

**(1) 調が動いて聞こえたのは、ヒジャーズで使ってはいけない度数の和音を鳴らしていたから。**
ヒジャーズ（C なら C, D♭, E, F, G, A♭, B♭）の上に三和音を積むと、度数によって性格が激変する。

| 度数 | 積んだ和音 | |
|---|---|---|
| 0 | C E G = **長三和音** | 主音。ヒジャーズの軸 |
| 1 | D♭ F A♭ = **長三和音** | ♭2。ヒジャーズの決め手（アンダルシア終止） |
| 3 | F A♭ C = **短三和音** | ♭4。これも定番 |
| 2 / 4 | E G B♭ / G B♭ D♭ = **減三和音** | 不安定 |
| **5** | **A♭ C E = 増三和音** | **調が動いて聞こえる正体** |

進行表が度数4・5・6を使っていたため、主音以外の和音が61%を占め、**36%の曲は1小節目からして
主音ではなかった**。旋法の音楽で軸から離れ続ければ、転調に聞こえて当然。

進行表を 0・1・3 だけに作り直し、全部を主音始まりにした。

| 民族200曲 | 前 | 後 |
|---|---|---|
| 和音の度数 | 0:39% / 3:27% / 4:16% / 5:13% / 6:6% | **0:57% / 1:24% / 3:19%** |
| 主音以外の和音 | 61% | **43%** |
| 1小節目が主音でない曲 | **36%** | **0%** |

**(2) 「ジャラーン」は、撥弦を一本ずつずらして駆け上げる。** 4小節ごとに、音階を5音
駆け上がる撥弦を内声に足した。間隔は0.07拍（テンポ76で約55ミリ秒）、長さ2.6拍。
シタールの release は1.2秒あるので鳴り続ける。

**音域を50〜61に限ったのが要点。** 同梱シタールは1音（実音 MIDI 52.4）しかないので、
高く置くと早回しで細くなる。この窓なら**原音から−2.4〜+5.6半音（中央値1.6半音）**で、
シタールが初めて地の音域で鳴る（旋律・内声は+8〜+31半音で鳴っている）。
内声は62〜73なので音域が重ならない ＝ 同じパート・同じ音高の重なり（テストが禁じている）も起きない。

実測：1曲あたり10音（5音×2回）、鳴らない曲0。seed3 の1回目は
MIDI 52 → 53 → 55 → 56 → 58 が 0.00 / 0.06 / 0.11 / 0.17 / 0.22 秒に並ぶ。

**テストを足した。** 進行表が 0・1・3 だけで全部主音始まりであること、ジャラーンが4音以上あり、
0.04〜0.12拍間隔で駆け上がり、内声と音域が重ならないこと。782ケースPASS。

**同じ日に「じゃ～んが音が変な重なりかたをしてうるさい」と言われて、直した。** 音階を順に
駆け上げていたのが誤り。5音が6半音のなかに収まり、それが2.6拍＋余韻1.2秒ぶん**全部同時に
鳴り続けていた**。ただの音の塊で、しかも非和音の音が和音パートともぶつかっていた。

| 同時に鳴る音どうしの間隔 | 前（音階順） | 後（和音構成音） |
|---|---|---|
| 1半音 | **50%** | 0% |
| 2半音 | 42% | 0% |
| 3半音 | 8% | 33% |
| 4半音 | 0% | 33% |
| 5半音 | 0% | 33% |
| **2半音以下でぶつかる割合** | **92%** | **0%** |

**弦は音階順ではなく和音の音に張られている。** 拾うのを `d, d+2, d+4` とその上のオクターブ
（`[0,2,4,7,9,11,14]`）だけにし、音域の窓を45〜61に広げて1オクターブ以上に散らした。4音。
実例：seed1 は A2→C3→F3→A3、seed3 は C3→E3→G3→C4。音量も少し下げた（`32+energy*12-k*2`）。

早回しは **中央値0.6半音・範囲−7.4〜+8.6半音**。シタールがほぼ地の音程で鳴る。

**テストを足した。** 5つの種で、同時に鳴る音の間隔が**すべて3半音以上**であること。
音階順に戻すと92%が2半音以下になって落ちる。

**三度目。「じゃらんの音が不自然」「じゃんとなったあともコードがたくさん続くのが違和感」。**
じゃらんの1発目から3秒のあいだに何が鳴っているかを、余韻（release）まで含めて足し算した。

| じゃらん直後3秒に鳴っている延べ秒数（100曲平均） | 前 | 後 |
|---|---|---|
| **じゃらん** | **11.67秒** | **4.55秒** |
| 和音（パッド） | 10.06秒 | 10.06秒（未変更） |
| 内声 | 4.08秒 | 4.08秒 |
| 低音 | 3.18秒 | 3.18秒 |

**撥弦のつもりが、4音の和音を2秒押さえっぱなしにしていた。** シタールの `release` は1.2秒
あるので、音符そのものを短くして余韻に任せればいい。音符の長さを **2.6拍→0.4拍** にした。

**不自然さのほうは、等間隔の4連打が機械に聞こえていた。** 3音に減らし、間隔を
0.07拍の等間隔から **[0, .035, .062]拍**（0・28・49ミリ秒）の詰まった不等間隔にし、
音量も 42→34→28 と減らして一つの動作に聞こえるようにした。左右も −.35 / 0 / +.35 に広げた。

和音構成音を散らす方針は変えていないので、同時に鳴る音の間隔は3〜5半音のまま（2半音以下は0%）。

テストに「撥弦は0.6拍より長くしない」を足した。押さえっぱなしに戻すと落ちる。

**未確認：聴いて自然になったか。** 残っている摘みは、じゃらん側は音数（3音）・間隔・長さ（0.4拍）・
音量、それでも「コードが続く」なら**パッドの `SCENES.ethnic.hold`（いま2.8拍）** が次。
ミステリアス寄りにするとき 1.1→2.8 に伸ばした値なので、戻すと明るさが戻る点に注意。

### 2026-09-20 · 民族を全体的にミステリアス寄りにする（明るさの正体は音階ではなく密度だった）

「民族が明るすぎるので、全体的にミステリアス寄りにしたい」。ヒジャーズにした直後の指摘。

**場面どうしを同じ物差しで並べたら、一目で分かった。** 民族は**このツールで一番忙しい場面**だった。

| | 打楽器 | 音符 | 和音1音 | テンポ | 波形 |
|---|---|---|---|---|---|
| **民族（前）** | **120発/分** | **5.31個/秒** | **0.69秒** | 96 | triangle |
| 街・酒場（賑やかな場面） | 95発/分 | 3.81個/秒 | — | 76 | triangle |
| 神楽 | 62発/分 | 2.77個/秒 | 4.11秒 | 76 | sine |
| 和風 | 0発/分 | 2.11個/秒 | 1.89秒 | 76 | triangle |
| 夜空 | 0発/分 | 2.54個/秒 | 0.95秒 | 76 | sine |

**賑やかなはずの街・酒場より打楽器が多く、音符も多かった。** 音階をヒジャーズにしても明るいままだった
のはこれが理由で、明るさの正体は旋法ではなく**密度とテンポと打楽器の跳ね**だった。

**変えたのは5つ。**

| | 前 | 後 |
|---|---|---|
| テンポ | 96 | **76**（`TEMPOS` にある値） |
| 波形 | triangle（明るさ係数1） | **sine（0.82）** |
| 元気 / 密度 | .65 / .65 | **.5 / .45** |
| 打楽器 | swing（三連で跳ねる） | **distant（2小節に1発の低い太鼓）** |
| 和音の `hold` | 1.1拍 | **2.8拍** |
| 民族弦の打楽器音量 | .9 | **.6** |

**結果**

| 民族 | 前 | 後 |
|---|---|---|
| 打楽器 | 120発/分 | **10発/分** |
| 音符 | 5.31個/秒 | **2.78個/秒**（神楽と同じ） |
| 和音1音の長さ | 0.69秒 | **2.09秒** |

**打楽器の選択肢は測ってある**（民族・テンポ76での実測）。distant が空きすぎるなら、この順で戻せる。

| | 発/分 | 平均の強さ |
|---|---|---|
| distant（採用） | 10 | 41 |
| light | 38 | 29 |
| ticks | 57 | 26 |
| swing（前） | 95 | 30 |

説明文にも「遠い太鼓がときおり鳴ります」を足した。

**未確認：聴いてミステリアスになったか。** 空きすぎるなら打楽器を light か ticks に。
まだ明るいなら `energy`/`density` をもう一段（.42/.38 あたり）、`FADE.ethnic` を足して
持続音を引かせる手もある。

### 2026-09-20 · 民族を東方旋法（ヒジャーズ）にする

「民族をもうすこしオリエンタルでミステリアスな要素がほしい」。

**「オリエンタル」を engine の数字に翻訳した。** 東方の音楽がそう聞こえる正体は音階にある。
ミクソリディア（全音が多い明るい旋法）では、どう並べても民謡になる。ヒジャーズ
（フリジア旋法の第3音を上げたもの＝和声的短音階の第5旋法）は **♭2と3のあいだが3半音（増2度）**
あり、これが「オリエンタル」の正体。「ミステリアス」は和声の動きを止めて持続低音を敷くこと。

**変えたのは3つ。**

1. `MODES.phrygianDominant=[0,1,4,5,7,8,10]` を足し、`MOODS.ethnic.mode` をこれに
2. `SCENES.ethnic.bass` を `['walk','fifth','two']` → `['pedal','walk','fifth']`（編成のひとつを持続低音に）
3. `SCENES.ethnic.harmony` を `[1,2,1]` → `[2,2,4]`（和音の変化を減らす）

併せて `COLOUR.phrygianDominant=[[6,'plain'],[3,'sus'],[1,'seven']]`。機能和声で動かすと
旋法が壊れるので、素の三和音とsusを多めにして旋法のまま置く。説明文も書き換えた。

**計測**（民族200曲・旋律あり）

| | 前（ミクソリディア） | 後（ヒジャーズ） |
|---|---|---|
| 旋律の音度 | 主音 / 2 / 3 / 4 / 5 / 6 / ♭7 | 主音 / **♭2** / 3 / 4 / 5 / **♭6** / ♭7 |
| 隣り合う音が1半音 | 17.4% | **37.7%** |
| 隣り合う音が**3半音（増2度）** | 17.8% | **24.8%** |
| 隣り合う音が2半音（全音） | 53.9% | 21.1% |
| 4小節あたりの和音変化 | 2.67回 | **1.19回** |
| 低音が主音を保つ割合 | 33.2% | **54.6%** |

半音と増2度が増えて全音が減り、和声はほぼ止まり、低音の半分以上が主音に居座る。

**テストを足した。** 音階そのもの、旋律に増2度が21%以上・半音が30%以上出ること、
低音が主音を保つ割合が45%以上。ミクソリディアに戻すと落ちる。

**踏んだ落とし穴。** `MODES` は `vm` のサンドボックス側で作られた配列なので、
`assert.deepEqual`（strict）は中身が同じでも**原型が違って落ちる**。`join(',')` で比べること。
テストの既存箇所が `Array.from(...)` で包んでいるのはこのため。

**未確認：聴いてオリエンタルに聞こえるか。** 音色は民族弦（シタール）のまま。まだ足りないなら
テンポ（96→76。`TEMPOS` にある値から選ぶこと）、`pad` の `hold` を伸ばす、`energy`/`density` を
下げる、が次の摘み。逆に「癖が強すぎる」なら `harmony` を `[1,2,2]` に戻すところから。

### 2026-09-20 · 神楽鈴を足す（合成・4小節の頭で一振り）

「神楽鈴がシャンとなるといいな」。

**CC0に神楽鈴は無い。** Freesoundで「神楽鈴」「suzu」「kagura bell」はCC0で0件。近いのは
そりの鈴・ハンドベルだが、球に粒が入ったジングルベルなので音が違い、クリスマス寄りに聞こえる。
**合成にした**（短い打楽器なので、持続音と違って合成で作りやすい）。

**作り方**（`suzuTone`）。神楽鈴は小さな鈴の束で、ひと振りで十数個が数ミリ秒ずれて鳴る。
金属音をひとつ鳴らしても鈴にはならないので、粒を撒いて重ねる。

- 2.3〜6.5kHz の**非整数倍**の正弦波を14粒、**45ミリ秒**のあいだに散らして重ねる
- 粒ごとに減衰0.25〜0.90秒。触れ合う金属の「シャッ」として、先頭に3.8kHz以上の短い雑音（0.16秒）
- 粒の位置・音程・減衰は種から作った乱数で決めるので、**同じ曲は毎回同じに鳴る**

**置き場所**は `add(4,84,b,1.6,54,-.15)`。MIDI 84 は General MIDI の Belltree なので、
書き出したMIDIでも鈴として読める。**打楽器の枠の中**に置いたので、打楽器を切ると鈴も止まる
（part 4 は打楽器だけ、という約束をテストが見ているため）。

**踏んだ落とし穴。** 最初 `events()` 後半の `s.drums!=='none'` の塊に書いたが、**一度も鳴らなかった**。
神楽の打楽器は場面ごとの経路（`kicks` の表があるほう）で作られていて、後半の塊は伴奏が legacy の
ときだけ通る道だった。「打楽器が出ているのだからこの塊が動いている」と思い込んだのが誤り。

**計測**（神楽200曲・既定設定）

| | 値 |
|---|---|
| 鈴の回数 | 1曲あたり **2.0回**（8小節テーマの0小節目と4小節目）／鈴が無い曲 0曲 |
| 発音 | 1振りにつき オシレーター14本＋雑音1本（`render` を辿って確認） |
| 打楽器オフ時 | part 4 の音符 **0個**（鈴も止まる） |

**テストを足した。** 神楽40シードすべてに鈴があること、鈴が4小節の頭（16拍の倍数、humanizeの
ずれ±0.2拍以内）にあること、打楽器を切ると part 4 が空になること、発音経路に `suzuTone` が
繋がっていること。782ケースPASS。

**「もっとシャラシャラした音にしたい」と言われて、同じ日に振り直した。** 摘みは `SUZU` 表にまとめた。

| | 前（シャン） | 後（シャラシャラ） |
|---|---|---|
| 粒の数 `bells` | 14 | **28** |
| 撒く幅 `spread` | 45ミリ秒（実測39） | **190ミリ秒（実測183）** |
| 50ミリ秒ごとの粒数 | **14 / 0 / 0 / 0**（全部が頭に固まる＝一発） | **10 / 7 / 5 / 6**（200ミリ秒擦れ続ける） |
| 雑音 `noise` | 1発（0.16秒） | **5発**（0.05〜0.10秒を撒く） |
| 1粒の音量 | .0175〜.0318 | .0087〜.0197 |
| 粒の音量の合計 | 0.370 | 0.403 |
| 合計エネルギー | 0.00485 | **0.00341** |

粒を倍にしても**うるさくならない**よう、1粒の音量を下げて総量を揃えている（エネルギーはむしろ減）。
左右への散らし（`width`）も足した。

**`skew` を入れた。** 粒を撒く偏り。最初 `Math.pow(random(),.7)` と書いていたが、1未満の乗数は
値を大きくするので**後ろ寄りに溜まっていた**（5/8/7/8）。振りは最初の接触が一番密なので
`skew:1.3` に直した（10/7/5/6）。

**三度目。「最初にシャランと一発なるのが気になる」「カーンって感じの音に聞こえる」。** 2つ直した。

**(1) カーンの原因は、粒を正弦波にして長く伸ばしていたこと。** 純音を0.98秒も伸ばせば、
小さな鈴ではなく叩いた金属棒になる。小さい鈴は質量が小さいので速く減衰し、音も高く、
純音ではなく帯域の狭い当たり音になる。粒の作りを入れ替えた。

| | 前（カーン） | 後（鈴） |
|---|---|---|
| 粒の本体 | 正弦波28個 | **帯域通過した雑音26個**（バンドパス Q7〜15） |
| 余韻 | なし（本体がそのまま伸びる） | 正弦波8個、本体の8分の1の音量 |
| 1粒の減衰 | 180〜980ミリ秒 | **51〜312ミリ秒** |
| 鳴り終わりまで | 約1.2秒 | **440ミリ秒** |
| 帯域 | 2300〜6500Hz | **3300〜8300Hz** |

バンドパスは中心以外を落とすので、同じ数字でも正弦波よりずっと小さく出る。白色雑音を
中心 f0・尖り Q に通したときの実効値はおよそ √((f0/Q)/(標本化周波数/2))。Q=10・f0=5kHz で
約0.15倍なので、`pingLevel` は正弦波の粒より一桁大きくしてある。**この見積もりは計算であって
実測ではない**ので、音量は耳で確かめて調整すること。

**(2) 鈴を4小節の「頭」から2小節ずらした**（`bar%4===0` → `bar%4===2`）。頭に置くと曲が鈴で
始まるため。間隔は4小節のまま、1曲2回。seed 7 では拍8.0と24.0に鳴る（前は0と16）。
併せて `skew` を1.3→1に戻し、振りの中でも頭に固まらないようにした（50ミリ秒ごとに
10/12/11/5 と均される）。

**未確認：聴いて鈴に聞こえるか。** 摘みは `SUZU` 表。`pingLevel`（当たり音の音量・要調整）、
`pingDecay`（伸ばすとカーンに戻る）、`hz`（帯域）、`q`（尖り。上げるほど音程を感じる）、
`pings`/`tails`（粒の数）、`spread`/`skew`（撒き方）、`width`（左右）。

### 2026-09-20 · 神楽の笛を実録音にする（CC0の原音2本を測って選んだ／埋もれを直した）

「神楽の篠笛のサンプリングまだだったよね」。合成の神楽笛で代用したままだった。

**CC0の篠笛そのものは存在しない。** Freesoundで「篠笛」「shinobue」「ryuteki / nohkan / fue」は
CC0で0件。VCSL（VSCO2と同じVersilianのCC0ライブラリ）にも和楽器の笛は無い。CC0の笛は2本だけ
だったので、**両方落として同じ物差しで測って決めた**（耳では差がつかなかった）。

| | A [365258](https://freesound.org/people/RutgerMuller/sounds/365258/) RutgerMuller | B [553217](https://freesound.org/people/t-man95/sounds/553217/) t-man95 |
|---|---|---|
| 形式 | 96kHz/24bit ステレオ 89.5秒 | 44.1kHz/16bit **モノ** 125.7秒 |
| 安定して鳴っている割合 | 34.2% | **75.9%** |
| 0.8秒以上の安定区間 | 13本 / 計14.2秒 / 最長1.70秒 | **44本 / 計48.9秒 / 最長2.40秒** |
| 音高の束 | 5個。MIDI 71.7〜74.6 と83付近（**間が9半音空く**） | **14個。MIDI 59.9〜86.0 を連続して覆う** |
| 倍音率の中央値 | 0.52 | **0.60** |

Aは箏と同じ録音セッション（箏=365242、笛=365258）だが**音域を覆えない**ので不採用。**B採用。**

**`scripts/build_shinobue.py` を新規に書いた。** 笛は減衰しないので箏の「撥弦の頭を切り出す」
方式は使えない。**合唱と同じループ点方式**（安定区間にループ点を打ち、整数周期ぶん離して
ゼロ交差に寄せる）。条件は、音程が0.8秒以上±25セント以内で安定・倍音率0.45以上・
区間内で音量が落ち込まないこと。**安定区間25本 / 音高の束11個 → 8音を収録**（bank.js 0.75MB）。

| root | 59.88 | 66.68 | 68.63 | 71.55 | 73.61 | 78.61 | 80.51 | 83.97 |
|---|---|---|---|---|---|---|---|---|
| 倍音率 | .65 | .72 | .72 | .68 | .67 | .64 | .63 | .59 |
| ループ秒 | .71 | .71 | .71 | .71 | .71 | .71 | .61 | .41 |

**神楽は既定で旋律を鳴らさない**（`DEFAULTS.kagura` に lead が無い）。`RECORDED.shinobue` の
`parts` を合唱と同じ `[0]` にすると**笛が一度も鳴らない**。箏と同じ `[0,3]` にしてテストで固定した。

**「笛がきこえない」と言われて、埋もれを計測した。** 発音経路をNodeで辿ったところ、笛は
**正しく16発ループ再生されていた**（種類も shinobue、早回し1.03〜1.08倍）。問題は音量と分量。

| 神楽・seed 7・25秒 | 音符 | 合計秒 | ピーク合計 | 最大ピーク |
|---|---|---|---|---|
| 和音(1) | 12 | 49.3 | 0.214 | 0.0119 |
| 低音(2) | 24 | 13.3 | 1.491 | 0.0567 |
| **笛＝内声(3)** | 16 | **10.1** | **0.651** | 0.0473 |
| 打楽器(4) | 24 | 7.6 | **4.072** | **0.2340** |

笛は伴奏（和音+低音）の**0.38倍**、打楽器の6分の1。しかも**25秒中10.1秒しか鳴っていない**。

**直した。** `LEAD_LIFT={shinobue:2.2}` を `partTrim` に足し、内声に回った笛だけ持ち上げる
（合唱の旋律を `.25` に下げるのと同じ仕組み・同じ関数なので、synthNote / sampleNote / chipTone の
3経路すべてに掛かる）。併せて `VOICES.shinobue.drums` を .65→.5。

| | 前 | 後 |
|---|---|---|
| 笛のピーク合計 | 0.651 | **1.432** |
| 伴奏比 | 0.38倍 | **0.84倍** |
| 笛の最大ピーク | 0.0473 | **0.1041**（低音0.0567を上回る） |
| 打楽器のピーク合計 | 4.072 | 3.132 |

**音量では解けない残りがある。** 笛が鳴るのは依然として25秒中10.1秒（40%）。旋律を出せば
112%になるが、`lead` を立てると内声の音域が**MIDI 47**まで下がり（`innerHigh=melodyLow-4`）、
同梱の最低音 59.9 から12.9半音の早回しになる。笛として成立しないので、やるなら
「旋律だけ実録音、内声は合成のまま」に作り分けること。

**テストを足した。** 笛の root が波形の実測と±0.5半音以内（実測ずれは最大0.08半音）、ループ点が
立っていて0.2秒以上あり収録内に収まること、**笛が内声(3)に割り当たっていること**。782ケースPASS。

**音色名について。** 原音の投稿者は「オリエンタル風の笛」としか書いておらず**何の笛か不明**。
`CREDITS.md` にはそのまま書いた。画面の音色名は「神楽笛」のまま（録音についての主張ではなく
狙いの音の呼び名。合成音のときから同じ名前）。正確さを優先するなら `SOUNDS` の `name` を変えること。

**未確認：聴いて神楽が成立しているか。** ループの継ぎ目、笛と太鼓の釣り合い。

### 2026-09-20 · 箏の同梱音源を作り直す（音高表示が最大18.7半音ずれていた／「ぴーん」の正体）

「まだ時々ピーンがはいる。特に最後のメロディー音」。同じ日の早回し対策（ひとつ下の項）は
+8半音以上しか見ておらず、これには効いていなかった。**原因は音源そのものだった。**

**計測① 同梱5音の実測音高**（bank.jsを復号し、調和積スペクトルで基音を測り直した）

| 音源 | 表示 | 実測 | ずれ |
|---|---|---|---|
| #0 | 39.79 | 58.52 | **+18.73半音** |
| #1 | 51.49 | 63.51 | **+12.02半音**（オクターブ誤認） |
| #2 | 62.48 | 63.51 | **+1.03半音** |
| #3 | 67.57 | 63.53 | **−4.03半音**（2本の弦が同時に鳴る収録） |
| #4 | 70.42 | 70.39 | −0.03半音 |

旧 `build_koto.py` は自己相関だけで音程を決めていた。自己相関は基音の整数分の1でも山が立つので、
倍音や隣の弦を基音と誤認する。`sampleNote` はこの表示を信じて早回しするので、**和風は
#2を使う音が1半音高く、#3を使う音が4半音低く鳴っていた**。2026-09-20 の「跳躍を半音で抑える」
修正で楽譜の跳ねは直っていたが、鳴っている音程が外れていたのはこれが原因。

**計測② #4 の音質**

| | #0〜#3 | **#4** |
|---|---|---|
| スペクトル重心 | 748〜826 Hz | **1078 Hz** |
| 4kHz以上の比率 | 0.013〜0.022 | **0.038** |
| 第2倍音／基音 | 0.09〜0.53 | **0.02** |

倍音をほとんど持たない細い高音。これが「ぴーん」の実体。

**計測③ 誰が鳴らしていたか**（和風400曲、旋律＝part 0）

| 使う音源 | 全体 | 曲の最後の音 |
|---|---|---|
| #2（+1半音で鳴る） | 22.1% | 29.8% |
| #3（−4半音で鳴る） | 21.7% | 23.3% |
| **#4（ぴーん）** | **56.1%** | **47.0%** |

#0と#1は表示が低すぎて旋律では一度も選ばれない死に音だった。旋律の半分以上が#4で、
最後の音は長く伸ばすぶん剥き出しになる。「特に最後のメロディー音」と一致する。

**`scripts/build_koto.py` を作り直した。** 採用の条件は4つ。

1. **音程の検算** — 自己相関をやめ、調和積スペクトル（HPS）で推定。立ち上がり・その少し後・
   余韻の3窓すべてで±20セント以内に一致すること
2. **収録の最大振幅が立ち上がりにある** — 後ろに強い撥弦があると、ピーク正規化がそちらに
   合わさって狙った音が小さくなる（＝実質「別の音の収録」）
3. **単音** — 基音の倍音列から外れた強いピークが2本以下（判定幅は±3%。±6%だと302Hzに対して
   320Hzが「倍音」に見える）
4. **倍音があり、明るすぎない** — （第2+3+4倍音）÷基音 ≥ 0.10、4kHz以上 ≤ 0.09

**収録長を2.5秒→1.5秒に縮めた。** 原音は13分の連続した即興なので、2.5秒まるごと他の弦が
鳴らない箇所がほとんど無い（条件を満たす音が2つしか残らず、音域も55.6と69.5だけになった）。
1.5秒なら10音・7束が残り、音域を覆える。長い余韻より「最初に鳴るのが狙った音である」を採った。

**結果**（同じ測り方で比較）

| | 旧バンク | 新バンク |
|---|---|---|
| 音高表示のずれ | −4.03〜+18.73半音 | **−0.03〜+0.01半音** |
| 倍音列に乗るエネルギー | 未計測（条件に無かった） | **0.64〜0.85** |
| 4kHz以上の比率 | 0.013〜0.038 | 0.020〜0.042 |
| 収録音数 / bank.js | 5音 / 1.07MB | 6音 / **0.77MB** |
| 収録 root | 39.8, 51.5, 62.5, 67.6, 70.4 | **50.5, 57.6, 62.5, 63.5, 67.8, 69.5** |
| 旋律の早回し量（絶対値） | — | 中央値1.49半音 / 最大6.49半音 |

単体版は 20.4MB → 20.1MB に減った。

**テストを2本足した。**（`tests/scene-variation.cjs`）

- 同梱音源の `root` が波形の実測と±0.5半音以内。テスト側にも小さなFFTとHPSを実装した。
  旧バンクを入れると最大18.73半音で落ちることを確認済み。**これが今回の本命の退行検査。**
- manifest の音質指標（上倍音/基音 ≥ 0.10、4kHz以上 ≤ 0.09、倍音率 ≥ 0.40）が範囲内。

**聴いて確認済み（2026-09-20）。** 「弦らしさは薄くなったが変な音が混ざらなくなったので
これで行く」。**採用。** 予告した副作用のうち (2) 余韻の短さが「弦らしさが薄い」として
出ている。トレードオフを承知のうえでの採用なので、戻すなら `CROP` ではなく別の手を探すこと
（§6.0 の「箏の弦らしさ」を見ること）。

### 2026-09-20 · 和風の「ぴーん」を消す（箏を音源の音域より上へ早回ししない）

「和風の音でときどき、ぴーん　みたいな音がまざる。1回くらいならいいけど何度も入ると気になる」。

**再現した。** 和風の既定設定で400曲（30秒）を作り、箏が鳴る旋律と内声の音符12,545個について、
その音を出すのに使われるサンプルからの早回し量（半音）を集計した。

| 早回し量 | 音符数 | 割合 |
|---|---|---|
| −5〜+6半音 | 12,219 | 97.4% |
| **+8〜+18半音** | **326** | **2.6%** |

出方は1曲ごとに偏る。**75%の曲は0回**、出るときは**1〜10回に固まる**（最大は10回／30秒）。
「ときどき混ざる」「何度も入ると気になる」という感じ方と一致する。

**原因は同梱音源の音域と旋律の音域が合っていないこと。** 箏の同梱は5音しかない。

| 同梱サンプルの音高 | MIDI 39.8 / 51.5 / 62.5 / 67.6 / **70.4** |
|---|---|
| 和風の旋律が届く音高 | 最高 **MIDI 88** |

`sampleNote` は一番近いサンプルを早回しして鳴らすので、MIDI 78以上はすべて最高音（70.4）を
+8〜+18半音で再生することになる。再生速度が上がるぶん **2.5秒の収録が1秒前後に縮み**、
余韻ごと細く硬くなる。これが「ぴーん」の正体で、**音そのものではなく早回しの副作用**だった。

**直し方は発音側で折り返す。** `SAMPLE_LIFT={koto:7}` と `foldToBank()` を足し、
音源の上限から+7半音を超える音はオクターブ下げて鳴らす（跳躍の制限と同じく**半音**で書く。§5参照）。
楽譜は一切変えないので、既存シードの曲の骨格はそのまま。

| 和風400曲・箏の音符12,545個 | 直す前 | 直した後 |
|---|---|---|
| +7半音を超える早回し | 326個（2.6%） | **0個（0.0%）** |
| 最大の早回し | **17.6半音** | **5.6半音** |
| オクターブ下げた音符 | — | 326個（2.6%） |

**シタールと合唱は対象にしない。** 同梱が1音だけ（シタール MIDI 52.4／合唱 53.3）で、
どの音も一律に+8〜+33半音で早回しされる。ばらつきがなく、それが音色として成立しているため。
`SAMPLE_LIFT` に載せた音色だけが折り返される。

テストは `tests/scene-variation.cjs` に追加。和風200シードの箏の音符が音源の上限+7半音を
超えないこと、シタールと合唱が折り返されないことを見る。`SAMPLE_LIFT` を空にすると
最大17.6半音に戻って落ちることを確認済み。

**未確認：聴いて「ぴーん」が消えたか。** 旋律の頂点が1オクターブ下がる箇所が2.6%出るので、
山が平らに聞こえないかも併せて確かめてもらうこと。

### 2026-09-20 · 和風の旋律が跳ねすぎるのを直す（跳躍を度数ではなく半音で抑える）

「和風で時々、特に最後らへんですごく音をはずす」。

**再現した。** 隣り合う旋律音の跳躍（半音）を40シードぶん集めると、和風だけが突出していた。

| 場面 | 音階 | 中央値 | 最大 | 7半音以上 | 四分割（前→後） |
|---|---|---|---|---|---|
| **和風** | **5音** | **3半音** | **10半音** | **16.3%** | 14% / 16% / **24%** / 16% |
| 民族 | 7音 | 2半音 | 7半音 | 4.3% | 1% / 5% / 9% / 4% |
| 儀式 | 7音 | 2半音 | 8半音 | 2.9% | 0% / 5% / 5% / 1% |
| 思索 | 7音 | 2半音 | 7半音 | 4.3% | 3% / 3% / 8% / 6% |

第3四半期が24%で最大。「特に最後らへん」という指摘と一致する。

**原因は跳躍の制限を「度数」で見ていたこと。** 度数は音階の音数で意味が変わる。

| 度数の差 | 7音音階 | 5音音階 |
|---|---|---|
| 3度 | 5半音 | **7半音** |
| 4度 | 7半音 | **9半音** |
| 和音構成音（度数0〜11）の幅 | 19半音 | **26半音** |

`if(Math.abs(d-previous)>4)` は「4度まで許す」つもりだが、5音音階では**9半音まで素通り**する。
さらに `development`（16小節テーマの13小節目以降）の `target+=2` も、5音音階では+4〜5半音になる。
これが「最後らへん」で跳ねていた正体。

**直した3点**（`makeMelody`）
1. `const semis=(a,b)=>Math.abs(pitch(s.scale,a)-pitch(s.scale,b))` を追加。
2. 大跳躍の判定を `Math.abs(d-previous)>4` → `semis(d,previous)>7` に。
3. 展開部の持ち上げを `development?2:0` → `development?(s.scale.length>=7?2:1):0` に。

**7音音階には実質影響しない。** 7音音階では4度＝7半音なので、`>4度` と `>7半音` は同じ境界になる。
実測でも民族・思索・回想・幻想は変化なし、儀式だけ 2.9%→2.3%（和声的短音階は増二度があるため）。

**結果**

| 場面 | 最大 | 7半音以上 | 四分割 |
|---|---|---|---|
| 和風 | 10半音 → **7半音** | 16.3% → **7.9%** | 24% → **13%**（第3四半期） |

残る7.9%は五音音階そのものの性質（隣の音でも3半音ある）で、消すと和風らしさが痩せる。
最大が他の場面と同じ7半音に揃ったので、ここで止めた。

**テストに固定した。** 和風・民族・思索で、旋律の跳躍が8半音以上にならないこと、
7半音以上が12%を超えないこと。度数の判定に戻すと `japanese の旋律に8半音以上の跳躍がある（最大 10）`
で落ちることを確認済み。

**注意**：和風（と、わずかに儀式）は同じシードでも以前と違う旋律になる。


### 2026-09-20 · 合唱をもう一段下げる（段階の目安つき）

「まだ声が大きい」。係数 `.38`→`.25`、音符内の減衰 `.45`→`.33`。

耳で「もう一段」と言えるよう、段ごとの効き目を測って表にした（儀式・実尺32秒・正規化を切った絶対RMS）。

| 係数 | 音符内の減衰 | 旋律−伴奏 | 旋律を足した上昇 |
|---|---|---|---|
| .38 | .45 | −0.6dB | 2.7dB |
| **.25** | **.33** | **−5.5dB（現在）** | 1.1dB |
| .18 | .30 | −8.7dB | 0.5dB |
| .12 | .28 | −12.5dB | 0.2dB |

触るのは `bgm-forge.js` の2か所だけ。`partTrim` の `score.sound==='choir'?.25:1` と
`SUSTAIN_FADE={choir:.33}`。テストの期待値（`sound==='choir'?.25:1`）も合わせること。

**計測中に見つけた潜在的な弱点**：`sampleBuffers` は復号後に `meta.pcm` を `delete` するため、
**同梱バンクは1ページにつき1回しか復号できない**。いまは `sampleCache` を捨てないので実害はないが、
キャッシュを作り直す経路を足すと `atob(undefined)` で落ちる。触るときは注意。


### 2026-09-20 · 合唱を旋律に使ったときの音量を下げる

「合唱部分の音量が大きく感じる」。

**最初に使った指標は外れた。** 旋律と伴奏を別々にレンダリングして（正規化を切り、絶対値で比較）
RMS比を取ると、儀式/合唱は **+6.4dB** で、幻想（+12.8dB）や夜空（+13.5dB）より**低かった**。
持続音と減衰音を同じRMSで比べても、聞こえ方とは一致しない。

**素材そのものを測って原因が分かった。**

| 素材 | ピーク | 全体RMS | 最後1秒のRMS | 長さ |
|---|---|---|---|---|
| シタール | 0.850 | 0.0579 | 0.0014 | 8.0秒（減衰しきる） |
| 箏 | 0.850 | 0.1723 | 0.1937 | 2.5秒（切って終わる） |
| **合唱** | 0.850 | 0.2067 | 0.1380 | 6.8秒（**ループして永久に続く**） |

合唱は3つの実録音のうち**唯一ループする**。しかも `sampleNote` は音符の間ずっと同じ音量を保つ作り
だった。**伴奏には `FADE` で減衰を入れたのに、旋律には何も入れていなかった。**
結果、伴奏が引いていく横で旋律だけが平らに鳴り続ける。

**直した2点**

1. `SUSTAIN_FADE={choir:.45}` を追加し、`sampleNote` が音符の中でも引くようにした。
   2秒かけて45%まで下げ、そこで保つ。**ループして伸び続ける音源だけが対象**で、
   撥弦（箏・シタール・ピアノ）は勝手に小さくなるので掛けない。
2. `partTrim` の旋律の係数を `.55`→`.38`。あわせて条件を **場面によらず `sound==='choir'`** に広げた。
   合唱は調整欄からどの場面にも選べるので、儀式だけに効かせる理由がない。

**計測（実尺32〜51秒・正規化を切って絶対RMS）**

| 場面 / 音色 | 旋律−伴奏（前） | 旋律−伴奏（後） |
|---|---|---|
| 儀式 / 合唱 | **+6.4dB** | **−0.6dB** |
| 和風 / 箏 | +1.7dB | （変更なし） |
| 民族 / シタール | −0.2dB | （変更なし） |

3つの実録音の旋律が同じ水準に揃った。旋律を足したときの全体の上昇も 7.3dB → 2.7dB。
係数だけなら −3.2dB の効果で、残りの約 −3.8dB は音符内の減衰によるもの。両方が効いている。

**聴いて確かめてください。** 引きすぎて声が埋もれるなら `partTrim` の `.38` を上げるか、
`SUSTAIN_FADE.choir` の `.45` を上げる（1に近いほど平ら）。


### 2026-09-20 · 箏と合唱もCC0の実録音に差し替え

シタールに続き、和風の「箏」と儀式の「合唱」を実録音にした。**3件ともCC0をこちらで再確認済み**
（Freesoundの各ページ、2026-09-20）。AGENTS.md §6 の「生成物がCC0と言い切れる」前提は保たれている。

**箏（Freesound 365242 / RutgerMuller）— 13分の即興演奏から単音を切り出した**
原音は演奏であって単音の録音ではない。`scripts/build_koto.py` が、直前0.24秒が静かで
（＝前の音が残っていない）、撥弦から0.6秒たっても同じ音程を保っている撥弦だけを拾う。
半音ごとに束ねて束の最良を1つ採り、音域が広くなるよう5音に散らす。乱数は使っていないので
同じ原音からは同じ5音が出る。候補36個 → 採用5音。

| 位置 | 周波数 | 実測ルート(MIDI) | 周期性 | 原音ピーク |
|---|---|---|---|---|
| 701.0秒 | 81.4Hz | 39.79 | 0.987 | 0.244 |
| 692.9秒 | 160.0Hz | 51.49 | 0.994 | 0.190 |
| 704.9秒 | 301.9Hz | 62.48 | 0.989 | 0.112 |
| 697.8秒 | 405.1Hz | 67.57 | 0.997 | 0.182 |
| 670.4秒 | 477.6Hz | 70.42 | 0.999 | 0.164 |

**箏は平均律ではない**（450/225Hz＝A+39セント、479Hz＝B−52セントなど）。ルートは小数のMIDI番号で
持たせている。音名から推測せず波形から測る運用（AGENTS.md §5）どおり。

**合唱（Freesound 764124 / ShangusBurger）— 4chアンビソニックのW成分だけを使う**
原音は Ambeo VR で録った4チャンネルB-format（FuMa）。無指向成分である先頭のWチャンネルだけを
取り出した。1.45〜8.30秒が音程の安定した区間で、**177.78Hz（F3 +31セント）・周期性0.980**。
主成分は356Hz＝2倍音で、和音ではなく単一の音程。群衆なので歌い手ごとに音程がばらつくが、
**それがコーラス感なのでそのまま残す**。
原音のピークは **0.072** と極端に小さく、正規化しないとほぼ聞こえない（フルートと同じ落とし穴）。
長い音でも伸ばせるよう、整数周期ぶん離したゼロ交差にループ点を置いた（1.197〜5.197秒）。

**配線**：音色ごとに「どのパートを実録音にするか」を `RECORDED` 表にまとめた。
シタールの `sitarBank` 特別扱いも表に吸収してある。

```js
const RECORDED={
  folk :{kinds:['sitar','strings','piano','sitar','drums'],parts:[0,3]},
  koto :{kinds:['koto','strings','piano','koto','drums'],  parts:[0,3]},
  choir:{kinds:['choir','strings','piano','piano','drums'],parts:[0]}
};
```
旋律（0）と内声（3）だけを実録音にし、和音と低音は合成音のまま土台にする。
合唱は旋律だけ。儀式の和音・低音は合成のコーラス（フォルマント付き）が引き続き支える。

**実音で確認**（単体版をヘッドレスChromiumで読み、`BGM_TEST.render`）。例外なし。

| 場面 | 音色 | 実尺 | peak | 継ぎ目の段差 | 旋律の音数 |
|---|---|---|---|---|---|
| 和風 | 箏 | 51秒 | 0.677 | 0.0008 | 16 |
| 儀式 | 合唱 | 32秒 | 0.565 | 0.0006 | 8 |
| 民族 | シタール | 40秒 | 0.807 | 0.0007 | 0（内声で使用） |

**テストを1か所直した。** 単体版に埋め込む `<script>` の本数をテストに書き写していたため、
音源を足すたびに古くなっていた。`bgm_forge.html` の `<script src>` から読む形にした。
以後、音源を足しても本数を書き換える必要はない。

**単体版は 18.0MB → 20.4MB**（箏1.07MB＋合唱0.58MB）。

**原音を `.gitignore` に入れた。** 琴の原音は448MBあり、`git add -A` で入るとGitHubが
100MB超のファイルを拒否してpushごと失敗する。4つの原音と `.crdownload`、macOSのエイリアスを除外した。

**聴いて確かめてください。** 箏は「和風」、合唱は「儀式」を選ぶと鳴ります。
気になる点があれば、切り出す位置や音数は `scripts/build_koto.py` の定数（CROP・WANT）で変えられます。


### 2026-09-20 · CC0実録音シタールを採用

- Freesound 42192 / deleted_user_229898 のユーザー提供FLACから先頭8秒を切り出し、モノラル32 kHz PCMを別bankに同梱。民族のfolk音色は表示名をシタールとし、旋律・内声を実録音に変更。低音・和音は従来合成。旧内声の人工ノイズは除去。
- 基本周波数169.052 Hz、fractional MIDI 52.4396を実測して調律。原音ピーク0.2703→0.85、末尾500 msフェード。原音40.8395秒→8秒、PCM 512000 bytes。出典・CC0・原音SHA256・加工条件をsamples/sitarに記録し単体版にもクレジットを埋め込み。
- Brave headlessのOfflineAudioContextでseed 2026、96 BPM、30秒指定をレンダリング。伴奏のみ／minimal旋律ありのpeak 0.7591／0.7126、左RMS 0.07078／0.07249、継ぎ目最大差0.000650／0.000457。NaNなし。単体版約18.0→18.7 MB。782ケースとPCM音高・ピーク・終端検査PASS。
- この計測は出力の健全性を確認するもの。楽器らしさは新しく民族を生成して鳴らし、人間が確かめること。

### 2026-09-20 · 音量調整を儀式のコーラスに限定

- 和風のリードは気にならないとの試聴結果から、minimal一律の0.70倍を撤回。和琴を含む他の組合せは1.0に戻し、儀式×コーラスのリードのみ0.55倍にする（直前0.70比で約−2.09 dB、未調整比−5.19 dB）。旋律密度を変えてもこの音量バランスを維持。
- コーラスは持続包絡と母音帯域フィルタを持つため和琴の減衰音より前に聞こえる可能性があるが、知覚音量の原因は未確定。今回は音色の形を維持してゲインだけ調整。
- 23場面×4音色×4密度で適用範囲と伴奏ゲイン不変を回帰検査。数値はレンダリング前係数であり完成音声RMSではない。新しく生成して鳴らし、聴感を確認すること。

### 2026-09-20 · ごく少ないリードの音量バランス

- ごく少ないは音数だけが変わり、リードの出力係数は他の密度と同じ1.0だった。音声レンダリングでpart 0のみ0.70倍（約−3.10 dB）に変更。合成・録音サンプル・チップ音源の3経路で同じ係数を適用する。
- 伴奏と打楽器の係数、音符の数・長さ・velocityは変更なし。MIDIのvelocityは従来通り。23場面でminimal/sparseのリード係数比0.70、他パート比1.0を検査する。
- この値はレンダリング前のゲイン比であり、完成音声のRMSや主観的な音量低下率ではない。新しく生成して鳴らし、バランスを確かめること。

### 2026-09-20 · 儀式の後半の持続音を短縮

- 儀式のみ、ループ末尾への最後の旋律音の強制延長を解除。コーラス・ごく少ないの既定を維持し、和音持続7.8→3拍、低音持続2.4拍、内声に4小節ごとの休符を設定。
- 同条件24 seed（1〜24、60 BPM、30秒指定、コーラス、ごく少ない、ループ）で旋律の最長持続6→2.2拍、3拍超の旋律24→0音、和音の合計note-beats 748.8→288。回帰検査に旋律・和音・低音の持続上限を追加。
- これは楽譜上の計測。残響込みの後半RMSと聴感は未検証であり、画像3曲そのもののseedも未取得。実際に鳴らして確認すること。

### 2026-09-20 · 儀式の長い持続音を途中で引ききる（FADE表に整理）

「後半で、長い音がずーっと続くときがうるさい。途中でフェード退場してほしい」。

**長い音の正体**：パッド（part1）と低音（part2）の **7.8拍＝7.8秒**。2小節ごとに鳴り直す。
これは全長に関わらず変わらない（実尺16/32/64秒のいずれでも最長は7.8拍）。

**前回の減衰では足りなかった理由**：`duration*.9` をかけて下げていたので、7.8秒の音は
7秒かけてようやく床に着く。**長い音ほどゆっくり下がる**という、いちばん困る挙動だった。
実音で測ると、音が始まって4秒後の時点でまだ **−6dB 程度**しか落ちていない。

**直し方は requiem が既に持っていた。** 音符の長さによらず一定の拍数で引ききり、あとは
静かな床に居座る（`Math.min(duration*.9, 2.4*beat)`）。これを儀式にも与えた。

ついでに、場面ごとの床の値がネストした三項演算子になっていたのを表に出した。

```js
// [どこまで下がるか, 何拍かけて下がるか]。拍数0＝音符の長さの9割（長い音ほどゆっくり）。
const FADE = { wonder:[.26,0], night:[.10,0], doubt:[.16,0], requiem:[.14,2.4], ritual:[.12,3] };
```

対象の場面一覧が `includes([...])` と三項演算子の2か所に重複していたのも、これで1か所になった。

**計測（実尺32秒・20ms窓のRMS包絡・音が始まって4秒後の落ち）**

| 版 | 4秒後の落ち | 最大から6dB以内 | 中央値(最大比) |
|---|---|---|---|
| 前（拍数なし・床.18） | −6.3 / −6.9 / −5.9 dB | 20.1% | −9.8dB |
| 3拍で引ききる・床.18 | −12.3 / −11.8 / −11.5 dB | 18.3% | −11.5dB |
| **3拍で引ききる・床.12（採用）** | **−15.3 / −13.5 / −14.0 dB** | 17.8% | −12.0dB |

4秒地点での落ちが **−6dB → −14dB 前後**、倍以上になった。

**波形の段差について**（指摘された画像の、中央で急に太くなる箇所）
これは長い音ではなく**旋律の入り**だった。既定の30秒指定は実尺16秒＝テーマ4小節で、
`phrasing:'minimal'` だと**前半に旋律が1音も入らない**（前半 part0=0音 / 後半 part0=4音）。
60秒・120秒指定では両方の半分に入るので段差は出ない。今回は触っていない。
気になるなら `makeMelody` の `shape` 判定を見ること。

**聴いて確かめてください。** 深すぎて持続音が消えて聞こえるなら床を `.18` に戻す
（`FADE.ritual` の1つ目）。引きが遅いと感じるなら拍数を `2` に（2つ目）。


### 2026-09-20 · 息継ぎを撤去し、儀式の持続音を減衰させる

**息継ぎ（`breathe`）は聴いて却下された。**「息継ぎ後の連続の長い音がうるさく感じる」。
隙間を作ったことで、そのあとに戻ってくる持続音の存在がかえって際立った。
仕掛けごと撤去してある（`SCENES` の `breathe` と `events()` の窓判定）。**再導入しないこと。**
計測上は効いていた（無音率 1.3%→9.4〜16.4%、実音の谷 −27.7dB）が、**効いていることと良いことは別**だった。

**本当の原因は持続音がずっと同じ音量だったこと。** 32秒の儀式を20ms窓のRMS包絡で測ると、
**最大から6dB以内に43.2%の時間いた**。下位10パーセンタイルでも最大比 −7.1dB しか下がらない。
つまりほぼ全編、最大音量の近くでべったり鳴っている。隙間の問題ではなく、起伏の問題だった。

**直し方は既にあった。** `render()` に「持続音を一定の音量で置かず、音符の中で減衰させる」処理が
あり、幻想・鎮魂・夜空・疑惑が使っていた。**儀式だけがこの一覧から漏れていた。**
一覧に `ritual` を足し、floor を .18 にした（夜空 .10 / 鎮魂 .14 / 疑惑 .16 / 幻想 .26 の間）。

| 版 | 最大から3dB以内 | 最大から6dB以内 | 中央値(最大比) | 下位10%(最大比) | peak |
|---|---|---|---|---|---|
| 減衰なし（前） | 13.3% | **43.2%** | −6.3dB | −7.1dB | 0.332 |
| floor .26（浅） | 8.1% | 20.8% | −9.3dB | −17.0dB | 0.422 |
| **floor .18（採用）** | 7.5% | **20.1%** | −9.8dB | −19.7dB | 0.434 |
| floor .10（深） | 6.9% | 19.4% | −10.5dB | −23.4dB | 0.447 |

べったり鳴っている時間が**43.2%→20.1%と半分以下**になった。peak は 0.332→0.434 に上がるが、
これは RMS を一定に揃える正規化の結果（起伏が増えると同じ RMS でも peak は高くなる）。
リミッタの 0.95 には十分余裕がある。

**説明文も直した。** 旧「重なった声と低い持続音が、2小節ごとに短く息を継ぎます。」→
新「重なった声が立ち上がり、低い持続音がゆっくり引いていきます。」

**まだ効かない場合の次の手**：儀式は `ACCOMP_TRIM` にも入っていない（伴奏の音量が 1.0 のまま。
幻想 .72 / 夜空 .62 / 疑惑 .68）。旋律がコーラスで入るようになったので、伴奏を下げて
声を通す余地がある。今回は触っていない。

**聴いて確かめてください。** 今回も、計測で言えるのは「起伏が増えた」までです。


### 2026-09-20 · 儀式に息継ぎを入れ、コーラスにフォルマントを足す

「音がずっと連続で疲れる」という指摘を、**無音率と最長連続**に翻訳して直した。

**犯人は内声ではなくパッドと低音だった**
パート別に0.01拍刻みで塗ったところ、内声は77.5%、打楽器は90.5%すでに休んでいた。
鳴りっぱなしはパッドと低音で、どちらも `hold:7.8`（2小節＝8拍のうち7.8拍）。全体の無音率2.5%、
0.15秒以上の隙間は30秒間で2回だけ。伴奏を薄くしても（内声を減らし bass を hold に変えても）
無音率は1.3%のまま動かなかった。

**`hold` を短くする案は捨てた**（計測で悪化が分かったため）

| 案 | 無音率 | 最長連続 |
|---|---|---|
| 現状 hold 7.8 | 1.2% | 8.0秒 |
| hold 6.0 / bassHold 6.0 | 6.4% | **10.7秒（悪化）** |
| hold 5.0 / bassHold 5.0 | 7.9% | 10.7秒 |

パッドが切れても内声と低音が埋める。**全パートが同時に休まないと隙間にならない。**

**採用：`breathe:[N小節ごと,M拍]`** を `SCENES` に追加し、儀式を `[2,1.5]` にした。
実装は `events()` の `add` 一箇所。全パートの唯一の入口なので、前の小節から伸びてきた
持続音も窓の手前で断てる。ループの継ぎ目の小節（`turnBar`）だけは除く。

| 実尺 | 無音率 | 隙間(0.3拍以上) | 最長隙間 | 継ぎ目に音 |
|---|---|---|---|---|
| 16秒 | 1.3% → **9.4%** | 0 → 1回 | 1.5秒 | あり |
| 32秒 | 1.8% → **14.1%** | 0 → 3回 | 1.5秒 | あり |
| 64秒 | 2.1% → **16.4%** | 0 → 7回 | 1.5秒 | あり |

**実音でも確かめた**（ヘッドレスChromiumでレンダリングし、20ms窓のRMS包絡を取った）。
儀式・16秒ループで、息継ぎの窓（6.5〜8.0秒）の谷は**中央値比 −27.7dB**。曲全体の最小値と一致する。
残響（wet .58〜.66）と release（.7〜1.2秒）で埋まる心配をしたが、包絡ではっきり落ちている。
なお最後の小節（14.5〜16.0秒）は −1.9dB で落ちない。turnBar を除外している設計どおり。
**既定の30秒指定は実尺16秒＝4小節なので息継ぎは1回しか入らない。** 効果を確かめるなら60秒以上を選ぶこと。

※ 最初に「|振幅| < 0.005 が0.25秒以上続くか」で測ったが、これはゼロ交差を数えていただけで
何も分からなかった。包絡で測り直している。生波形のしきい値では連続性は測れない。

**コーラスにフォルマントを追加**
`synthNote` は倍音の足し算＋ローパス1本で、母音の共鳴を作る手段がなかった。
`formants:[[Hz,Q,量],…]` と `formantDry` を音色仕様に足し、ローパスの後段に帯域通過を並列で挿す。
コーラスは key に F1=620/F2=1080/F3=2600、pad に 500/900 を置き、波形を sine→sawtooth に変えた
（倍音がないとフォルマントが立たないため）。低音は濁るので掛けていない。
`formants` を持たない既存13音色は分岐に入らず、従来どおり素通り。実音レンダリングで例外なし、
peak 0.318、継ぎ目の段差 0.0061。

**儀式の説明文を実装に合わせた**
旧「低い持続音に、二連の太鼓が重なります。」→ 既定音色がコーラスになり、`drum:'none'` の結果
`kind` が `light` にフォールバックして軽い2・4拍が鳴るようになっていたため、記述が二重に古かった。
新「重なった声と低い持続音が、2小節ごとに短く息を継ぎます。」

**テストに固定した**（3件）：儀式の既定が `[60,'choir',true,'minimal']` であること、
1.4拍以上の息継ぎがあること、継ぎ目に音が残ること、`synthNote` がフォルマントに対応し
コーラスが `formants` を持つこと。いずれもわざと壊して落ちることを確認済み。782ケースPASS。

**聴いて確かめてください。** 計測で言えるのは「谷ができた」までで、疲れなくなったかは分かりません。


### 2026-09-20 · 「これから作る曲」に旋律量を表示

- 「ごく少ない」の既定値は設定されていたが、確認欄の表示用マップに含まれておらず「ふつう」と表示されていた。表示マップに「ごく少ない」を追加し、和風五音音階の表示も追加した。
- 単体版を再生成し、23場面・782ケースの回帰テストをPASS。

### 2026-09-20 · 和風選択時の既定を「ごく少ない」に変更

- 和風を選んだ時点で、メロディの既定が「ごく少ない」になるよう変更。ごく少ないの音数増加とループ終端の接続改善は維持した。
- 23場面・782ケースの回帰テストを実施。

### 2026-09-20 · 「ごく少ない」を少し増やし、ループ終端を接続

- 「ごく少ない」は4小節の入口だけを休符にし、中間の休符を解除。和風の旋律手掛かりを残しつつ、「少なめ」より少ない関係を維持した。
- ループ時の最終小節を旋律の終止判定から外し、次のループ先頭へ戻るターンアラウンドとして扱う。単体版を再生成し、23場面・782ケースPASS。

### 2026-09-20 · 和風の既定旋律を「少なめ」に戻す

- 「ごく少ない」は旋律の登場回数を減らせる一方、和風の五音音階と和琴の特徴まで聞こえにくくする可能性があるため、和風の既定だけを「少なめ」に戻した。「ごく少ない」は手動選択肢として残した。
- 「ごく少ない」専用の休符制御は維持。既定値の回帰検査と、和風の五音音階 `[0,2,4,7,9]` を確認し、単体版を再生成する。

### 2026-09-20 · 「ごく少ない」の旋律数逆転を修正

- 計測で和風の「ごく少ない」が24 seed合計433音、「少なめ」が367音となり、密度設定に反して逆転していた。ごく少ない専用に4小節の入口と中間を休符にし、旋律の登場箇所を明示的に減らした。
- 回帰検査で、ごく少ないの旋律数が少なめより少ないことを固定。単体版を再生成し、23場面・782ケースPASS。

### 2026-09-20 · 和風の旋律を「ごく少ない」へ、民族弦を再調整

- 和風の初期メロディ量を「少なめ」から「ごく少ない」へ変更。densityの下限を18%、基礎密度の42%にし、要所だけ旋律が現れる設定にした。
- 民族弦は2.01・3.02・5.03・8.07倍音と長めの余韻へ変更。シタールの共鳴弦を意図した調整で、内声の高域ノイズによるミュート・ジャラランも維持。
- 音階とスコアイベントの決定性を維持。単体版を再生成し、23場面・782ケースPASS。

### 2026-09-20 · 民族弦にシタール風の余韻とミュート弦を追加

- 民族弦の倍音を増やし、音程のある撥弦をシタール寄りに調整。民族場面の内声には高域ノイズを0.065秒だけ重ね、「無音に近いジャララン」の弦擦れを追加。
- 音階・スコアイベント・他の音色は変更していない。23場面・782ケースPASS、単体版を再生成。聴感は民族場面を実際に試聴して確認すること。

### 2026-09-20 · 和風を五音音階と少なめの旋律に変更

- 和風専用の五音音階 `[0,2,4,7,9]` を追加し、西洋7音階のミクソリディアンから変更。和風用の進行も3度の連続感を抑えた開放的な形へ変更。
- 和風は初期状態でメロディあり・「少なめ」に変更。和琴の硬質な余韻と、間を空けた伴奏を組み合わせる。
- `pitch()` を音階の長さに対応させ、既存の7音階と新しい5音階を共通処理。23場面・782ケースPASS。単体版を再生成。

### 2026-09-20 · 決断をメロディ少なめの初期設定に変更

- 「⚖️ 決断」だけ、場面選択時にメロディをオン、量を「少なめ」に設定。ほかの場面の初期状態は変更しない。
- 初期設定の回帰検査を追加。既存の23場面・782ケースに加えて、決断の初期設定を確認。単体版を再生成し、JS版に内容ハッシュを反映。

### 2026-09-20 · 民族・和風・神楽の専用音色を追加

- 民族弦、和琴、神楽笛の3音色をブラウザ合成で追加。民族弦は乾いた撥弦、和琴は硬質な余韻、神楽笛は息の混じる笛を意図した倍音・エンベロープ・フィルターを設定。
- 民族・和風・神楽の初期音色を、それぞれ専用音色へ変更。外部音源や通信は追加していない。作曲ロジックは維持し、単体版を再生成。23場面・782ケースPASS。

### 2026-09-20 · 民族・和風・決断・神楽を追加

- 4場面を追加。民族は異国の旅、和風は城下町や山道、決断は重大な選択、神楽は和風儀式に用途を分離。
- 民族はスチール弦、和風は木のマレット、決断はやわらかい電子音、神楽はフルートを初期音色に設定。各場面に専用の伴奏型・和音移動・音域・打楽器ルールを追加。
- 23場面・3構成・決定性・音域・プレビューなどの回帰テストを実行。単体版を再生成し、JS版に内容ハッシュを反映。

### 2026-09-20 · 調整欄の案内ラベルが切れる問題を修正

- 調整欄の親要素に設定していた `overflow:hidden` が、上端に出る案内ラベルを切っていたため `overflow:visible` に変更。スマホ幅での横幅制御は `min-width:0` と各入力要素の `box-sizing` で維持。
- 単体版を再生成し、646ケースPASS。

### 2026-09-20 · スマホの音色選択画面を短くする

- スマホのネイティブselect画面で音色名と説明文が全行表示され、縦に長くなっていたため、調整欄の音色選択肢を「音色名だけ」に変更。説明文は既存の「音づくり」詳細欄に残した。
- 作曲ロジックは変更せず、内容ハッシュ付きのJS版を更新。単体版を再生成し、646ケースPASS。

### 2026-09-20 · クリアの初期音色を木のマレットへ変更

- 「🎉 クリア」の初期音色を室内楽から木のマレットへ変更。説明にも木のマレットを反映し、明るく弾むクリア感を示す文面に更新。
- 作曲ロジックと伴奏型は変更せず、クリア固有の進行・リズムは維持。初期音色の回帰検査を追加。646ケースPASS、単体版を再生成。

### 2026-09-19 · クリア場面の説明をゲーム全般向けに調整

- 説明を「問題が解決した。事件が解決した。脅威が去った。探索や依頼をクリアした喜びに。」へ変更。特定のゲーム展開や「ピアノ」を前面に出さず、弾む伴奏と軽い打楽器による明るい達成感だけを案内する文面にした。
- 作曲ロジック・音色設定は変更せず、内容ハッシュ付きのJS版を更新。646ケースPASS。

### 2026-09-19 · 解決をアップテンポの「クリア」へ再調整

- 「もっと明るいクリアー！」という要望で、表示名を「🎉 クリア」、既定を76→116 BPM・柔らかいピアノ→室内楽に変更。短い裏拍混じりの内声と軽い打楽器を追加。4小節ごとの主和音到達は維持。
- seed 1〜24・30秒指定・伴奏のみ。同一96 BPMの1小節あたり内声2.250→4.750音、打楽器0→2音、低音1.333→2音。内声の指定長1.1→0.38拍。既定設定で全発音密度2.205→7.497音/秒。旧凱旋の行進キック・スネアは使わない。音質・聴感ではなく楽譜統計。
- 回帰検査を軽い打楽器あり・キックとスネアなし・内声4〜5音/小節・長さ0.4拍以下へ更新。646ケースPASS。820px・390px幅で名称・説明・初期設定の表示を確認。単体版を再生成、内容ハッシュ付きの版へ更新。聴感は利用者の試聴で確認すること。

### 2026-09-19 · 凱旋を探索シナリオ向けの「解決」へ変更

- 表示名・説明を「🌤️ 解決」に変更。内部ID victoryは維持。既定116 BPM・室内楽・打楽器driveから、76 BPM・柔らかいピアノ・打楽器なしへ。行進の低音を持続音中心にし、内声を疎に、パッドを小節頭から伸ばす形へ。全進行を4小節ごとに主和音へ着地させた。
- seed 1〜24、30秒指定、伴奏のみで計測。同じ96 BPMで1小節あたりパッド6.760→3.380音、低音3.333→1.333音、内声3.083→2.250音、打楽器6.250→0音。低音の平均長0.940→2.775拍。既定テンポで全発音密度9.390→2.205音/秒。音量や音質の実測ではなく楽譜統計。
- 24 seedで打楽器なし・低音2音/小節以下・内声3音/小節以下・4小節ごとの主和音到達を回帰検査に追加。既存646ケースも確認。820px・390pxで名称・説明・初期設定の表示を確認。単体版を再生成し、JS版に内容ハッシュを反映。
- 聴感評価は未実施。「解決」で新しく作曲して鳴らし、シナリオに合うか人間が確認すること。

### 2026-09-19 · 全19場面の説明に「音のイメージ」を1文足し、明るいを汎用に開く

**型を3文に統一した。** 具体例 → 使いどころ → 音のイメージ。19/19が3文になった（旧：2文が18本）。
音のイメージの一文は、実装値（旋法・既定テンポ・既定音色・`drums`・`SCENES` の bass/drum/gate）から
起こしていて、思いつきでは書いていない。

- **「のどか」だけ音の一文を冒頭に置いた**（「弾む三連のカントリー調。」）。カントリー調は
  この場面の性格そのもの（mixolydian ＋シャッフル＋スチール弦）で、先に言ったほうが選びやすい。
- **「明るい」を汎用に開いた**。旧「街道の移動、買い物や聞き込み、…」→
  新「移動、買い物、雑談、ひと息。明るい場面ならどれにでも。木のマレットが軽く跳ねます。」
- カジノと幻想は、文中にあった音の説明（「跳ねるピアノと軽快な低音で」「澄んだ鐘と浮遊する響きで」）を
  末尾の定位置へ移した。これで**音に言及する説明は 2/19 → 19/19、位置も揃った**。

**「音色は変えられるのに説明に書いてよいのか」について**
問題ない。`selectMood()` は場面を選んだ時点で `state.sound` と `state.bpm` を `DEFAULTS` に戻すので、
**説明カードが読まれる瞬間は常に既定音色が効いている**。調整欄で音色を変えられるのはそのあと。
既定音色と説明が名指しする音が全19本で一致していることは、スクリプトで確認済み。

**計測**
- 字数 31〜51字 → 41〜72字（平均56字）。
- ヘッドレスChromiumで最長の「疑惑」（72字）を820px・390pxで確認。
  820px＝2行・高さ48px、390px＝4行・高さ95px、どちらも横スクロールなし。折り返しの事故なし。
- 音・作曲ロジックは無変更。646ケースPASS。

**この修正で解消した積み残し**：音色に言及しているのがカジノと幻想だけ、という不統一。
**残っている積み残し**：疑惑↔思索の「調査」、荘厳↔鎮魂の「葬送」、悲哀の例に入った「回想」、
鎮魂と凱旋が自分自身の名前を例に入れている点。


### 2026-09-19 · 「明るい」「のどか」の説明を、例が同じ方向を向いている状態から開く

「説明が限定的すぎる」という指摘を、**縛っている軸の数**に翻訳して直した。
原因は「例が少ない」ことではなく、**例が全部同じ方向を向いている**ことだった。

**のどか** — 19本のうち、時間帯を二重に指定していた唯一の説明（`朝` と `日が高くなる前`）。
冒頭の3例も舞台＝田舎・時刻＝朝・局面＝出発前の一点に集まり、3軸とも固定されていた。
音は mixolydian ＋シャッフル＋スチール弦のカントリー調なので田舎っぽさは音に内在するが、
朝である必然性は実装のどこにもない。舞台だけ残して時刻と局面を外した。

- 旧：朝の畑と家畜の鈴、開いた窓、出発前の身支度。日が高くなる前ののんびりした時間に。（40字）
- 新：畑と家畜の鈴、開いた窓、井戸端の立ち話。急ぐ用事のない、のんびりした時間に。（38字）
- 時刻の指定 2→0、局面の指定 1→0。

**明るい** — 縛っていたのは時刻ではなく局面。報酬の受け取り／切り抜けたあとの一息／日常に戻ってきた場面と、
3要素とも「何かが終わったあと」に寄っていて、凱旋（依頼の達成、セッションの締め）と役割が重なっていた。
音は ionian・energy .75・light・96拍で、凱旋（energy 1・drive・116拍）よりはっきり穏やかなので、
日中に長く敷ける性格のほうが実装に合う。

- 旧：朝の街道、報酬の受け取り、無事に切り抜けたあとの一息。日常に戻ってきた場面に。（39字）
- 新：街道の移動、買い物や聞き込み、無事に切り抜けたあとの一息。特に何も起きていない、明るい時間に。（47字）
- 「あと／戻ってきた」系の語 3箇所→1箇所（一息だけ残した）。

**全体への影響**
- 時間帯を名指しする説明：4本 → 2本。残るのは「夜空」と「暗い」で、どちらも夜であることが
  場面の性格そのものなので縛りにならない。
- 字数は 31〜51字の既存の幅に収まる（明るい47字・のどか38字）。2文の場面は 18/19 を維持。
- 音・作曲ロジックは無変更。646ケースPASS。


### 2026-09-19 · 「思索」の説明から、存在しない場面への参照を外す

- 「思索」の説明が「**神秘が**謎めいた探索なら、こちらは頭を使う時間」と、統合済みで一覧に無い場面を
  引き合いに出していた。読んだ人が探しても見つからない場面と比較されている状態だった。
- 「幻想」に差し替える案もあったが、説明文は選んだ1場面ぶんしか画面に出ない（`renderBrief` が
  `#brief` に出すだけで、場面ボタンには名前しか出ない）ため、**比較相手の文を見られないまま読まれる**。
  対比そのものを外した。
- 57字・3文 → 37字・2文。これで2文の場面は 17/19 → 18/19（残る3文は「鎮魂」のみ）。
  19本の説明文のうち、廃止した場面名（神秘・まどろみ・穏やか）への参照は0件になった
  （「幻想」に残る「神秘的な遺跡の探索」は形容詞であって場面名の参照ではない）。
- 音・作曲ロジックは無変更。646ケースPASS。

**未着手（コードレビューで挙げた残り）**
- 「疑惑」と「思索」が両方とも「調査」を名乗っている。README も「疑惑＝調査パート用の場面」と書いていて
  思索の説明と衝突する。今回は触っていない。
- 「葬送」が荘厳と鎮魂の両方にある。荘厳は例に挙げた葬送を締めの一文で回収していない。
- 「悲哀」の例に、別の場面名である「回想」が入っている。
- 音色に言及しているのはカジノと幻想だけ（音色は調整欄で変えられるため、変えると説明と合わなくなる）。


### 2026-09-19 · 試聴レンダリングを実際に中止し、ビルド忘れをテストで止める

コードレビューで挙がった上位2件。音の出力は変えていない。

**1. 古い試聴レンダリングを止めた（`bgm-forge.js`）**

- `editFields` の change ごとに `trySample()` が走るが、`sampling` カウンタは結果を捨てるだけで、
  古い `render()` は最後まで回っていた。音色→テンポ→音量と続けて触ると、2小節ぶんとはいえ
  OfflineAudioContext が3〜4本並走する。`render` に `()=>turn!==sampling` を渡して実際に止めた。
- 前提として `render` の中止経路を直した。従来は `ctx.suspend()` のコールバックで `abortReject` を
  呼ぶだけで `ctx.resume()` を呼ばず、コンテキストが suspended のまま残り、`startRendering()` の
  Promise が永久に pending になっていた。その Promise から曲ぶんのノード（2分の曲で数千個）が
  参照され続けるため、中止のたびに解放されない塊が積み上がる。OfflineAudioContext は中断できないので、
  **resume して最後まで回しきり、結果を捨てる**のが唯一の解放手段。`aborted` フラグで reject は1回だけにした。
- 計測は未実施。ヒープの実測（中止5回の前後比較）は次の課題として残す。挙動の変化は
  「古い試聴が鳴らなくなる」だけで、646ケースPASS。

**2. 単体版の乖離と `?v=` の上げ忘れを、テストで落とすようにした（`tests/scene-variation.cjs`）**

- AGENTS.md §3 の手順1・2は人の記憶に頼っていて、実際に 2026-09-16 に乖離している。
  テストが見ていたのは MIT コメントとファビコンだけで、埋め込まれたJS本体は見ていなかった。
- 単体版から `<script>` ブロック4本を取り出し、分割ソースと文字列比較する assert を追加。
  ビルドを忘れると `python3 scripts/build_standalone.py を実行すること` と出て落ちる。
- `?v=` の末尾に、そのファイルのsha256先頭8桁を付ける規約にした。ずれると
  `?v=20260919-sample-cancel-37fb61d1 に直すこと` と、**貼るべき文字列をそのまま表示**して落ちる。
- 両方とも、わざと壊して落ちることを確認済み（分割ソースに1行足す／ハッシュを `deadbeef` に書き換える）。
  テスト件数は646ケースのまま、新規assertは6本。


### 2026-09-19 · 保存形式の使い方を補足

- 「保存する」を指定文面へ差し替え。WAV・MP3の選択、用途、編集用MIDIの補足を2つの明示的改行で区切った。案内する形式数は1→3。
- 820px・390px幅のスクリーンショットで説明の折り返しと配置を確認。版を `20260919-save-guide` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · OGPのマークをページと同じ比率へ拡大

- ページの大画面時のタイトル42px・マーク20×32pxを基準に、OGPタイトル146pxに対してマークを約69.5×111.2pxへ拡大（旧40×64px）。倍率146/42を適用し、Bの左で上下中央を維持。PNGを目視確認、1200×630pxを維持。
- 版を `20260919-ogp-mark-size` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · OGPに四角のロゴを追加

- OGPのBの左に40×64pxの水色ロゴを追加。段違いの2つの矩形を静止状態で配置し、タイトルの上下中央に合わせた。
- SVGからPNGを再生成し、1200×630pxを維持。画像を目視確認。版を `20260919-ogp-mark` に更新し単体版を再生成。646ケースPASS。

### 2026-09-19 · ロゴの2つの四角が入れ替わる動きを追加

- SVGを2つのrectに分割。16秒周期で左上と右下が1.6秒かけて入れ替わり、4.8秒静止してから1.6秒で元へ戻る。従来の穏やかな上下動も維持。
- 820px・390px幅で開始・途中・交換後・復帰の4時点を検証。移動量はそれぞれ(10,16)pxと(-10,-16)px、復帰後は両方(0,0)px。スクリーンショットで配置確認。動きを減らす設定で両rectのanimation-nameがnone。
- 版を `20260919-mark-swap` に更新し単体版を再生成。646ケースPASS。

### 2026-09-19 · タイトル左にアニメーション付きロゴを追加

- 提供画像の段違いの水色の形をインラインSVGで再現し、BGM FORGEのBの左に12pxの間隔で配置。ロゴ20×32px、6秒周期で上下2px・不透明度0.72〜1の緩やかな動き。外部画像の読み込み不要。
- 820px・390px幅のスクリーンショットで配置確認、横方向のはみ出し0。動きを減らす設定ではanimation-nameがnoneになることを両幅で確認。
- 版を `20260919-title-mark` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 使い方の見出しを淡い金色に変更

- 比較サンプルから選ばれた淡い金色 `#e4ad65` を5項目の見出しに適用（旧 `#eaf3f8`）。明朝体・16px・行高26pxを維持。
- 820px・390px幅のスクリーンショットで確認。番号枠と見出し行の中心差は全5項目0px。版を `20260919-help-gold` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 再作成の案内文を修正

- 「聴く」の説明1箇所を「もう一度押すと再作成されます」に変更。820px・390px幅のスクリーンショットで表示確認、番号と見出しの中心差は全5項目0px。
- 版を `20260919-help-recreate` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 使い方の見出しを明朝体へ変更

- 5項目の見出しを端末内の明朝体（ヒラギノ明朝・游明朝など）へ変更。16px・行高26pxを維持し、外部フォントの通信は追加していない。
- 820px・390px幅のスクリーンショットで表示確認。番号枠と見出し行の中心差は全5項目0px。版を `20260919-help-mincho` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 使い方の説明文に改行を追加

- 指定文面に合わせ、01・02・04の説明に明示的な改行を各1件、合計3件追加。「聴く」を「もう一度押すと作成されます」に変更。番号・見出しは5項目の構成を維持。
- 820px・390px幅のスクリーンショットで改行と配置を確認。番号枠と見出し行の中心差は全5項目0pxを維持。版を `20260919-help-breaks` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 使い方の番号と見出しの上下位置を統一

- 見出しの上マージン4pxをなくし、行高を番号枠と同じ26pxに指定。820px・390px幅とも、全5項目で番号枠と見出し行の中心差が3px→0px。スクリーンショットでも位置を確認。
- 版を `20260919-help-alignment` に更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 使い方の項目見出しを拡大

- 5つの項目見出しを13px→16px（約23%拡大）に変更。説明文は12pxのまま。版を `20260919-help-headings` に更新し、単体版を再生成。
- 646ケースPASS。ヘッドレスChromeで820px・390px幅の見出し全5件が16pxであることと、スクリーンショットで重なりがないことを確認。390px幅では既存のスクロール領域内に説明を表示。

### 2026-09-19 · 使い方にテイク数の上限を追記

- 「惜しければ直す」の末尾に、指定文言「（※最大１２個まで）」を追加。該当ステップの上限表記は0件→1件。スクリプトの版を `20260919-take-limit-guide` に更新し、単体版を再生成。
- 646ケースPASS。ヘッドレスChromeの820px・390px幅でスクリーンショットを確認し、追記部分の欠け・重なりなし。

### 2026-09-19 · 開発状況の整理

- §0に今回の最終仕様・検証・公開確認の範囲を集約し、ファイル表と配布構成を更新。READMEの音色数を実装の13種類へ訂正（旧記載11種類）、疑惑の説明も現行のwave伴奏に合わせた。646ケースPASS。
- 単体版は18,007,474 bytes（約18.0MB）。OGPは176,559 bytes、ファビコンは303 bytes。


計測方法: `node` で楽譜を大量生成して統計を取る／同じコードをヘッドレスChromiumで実レンダリングしてFFT・RMS・相関を取る。
詳細な数値は `CODE_REVIEW_V2.md` に全部残してある。

### 2026-09-19 · ファビコン

- ファビコン指定0件→分割版・単体版に各1件。黒地に画面の金色（`#e4ad65`）の連桁音符を描いた `favicon.svg`（303 bytes）を追加。
- 単体版ではビルド時にSVGをdata URIへ埋め込む。画像ファイルなしでも持ち運べる。分割版を配るときは `favicon.svg` も同梱する。
- `tests/scene-variation.cjs` に分割版の参照と単体版の埋め込み内容一致の検査を追加。646ケースPASS。
- ヘッドレスChromeで16/32/64/192pxのアイコンと820px・390px幅の画面を撮影し、形状と折り返しを確認。作曲・音声処理の変更なし。

### 2026-09-19 · OGP画像

- 共有画像指定0件→分割版・単体版に各1件。`ogp.svg` を編集元とし、1200×630pxの `ogp.png`（129,592 bytes）を追加。画面と同じ黒・金・青、上段に「BGM FORGE / THEME & SCENE EDITION」、中央に大きな「BGM FORGE」を配置。
- `og:title` / `og:description` / `og:url` / `og:image`・寸法・代替テキストと `twitter:card` を追加。画像URLはGitHub Pagesの絶対URL。公開後にSNSが取得する。ローカルの作曲・再生には通信不要。
- PNGの寸法と両HTMLの画像参照を回帰検査に追加、646ケースPASS。画像の文字と余白を目視確認。単体版を再生成。
- PNGの再生成: Nodeの `sharp` で `sharp('ogp.svg').png().toFile('ogp.png')` を実行（MenloフォントのあるMacで作成）。音声処理・画面本文の変更なし。

### 2026-09-19 · ダウンロードの案内文

- 保存欄のガイド1か所を「気に入ったら保存」（8文字）から「気に入ったらダウンロードする」（14文字）へ変更。分割版から単体版を再生成し、646ケースPASS。
- 820px・390px幅で案内表示時の折り返しを確認。処理の変更なし。

### 2026-09-19 · OGPにサービス説明を追加

- OGP下段の波形を説明2行に変更。「場面に合ったBGMを、ブラウザで自動作曲」「つくった曲は無料でダウンロードできる」を34px・30pxで配置。
- PNGは1200×630pxを維持、129,592→151,379 bytes。日本語の文字表示と余白を目視確認。代替テキストも更新し、単体版を再生成。646ケースPASS。

### 2026-09-19 · 操作ガイド3か所を具体化

- 再生・保存・調整のガイドを指定文言へ変更。再生は「作り直した曲を　聴いてみる」、調整は「この曲を微調整したい場合は、ここ」。
- 保存は「この曲がきにいったら、ここからダウンロード（保存ボタンをクリック）」へ。自然幅389.6pxをスマホでは296pxに制限して2行表示、上余白34pxを確保しボタンとの重なりを防いだ。820px・390pxのスクリーンショットで3か所を確認。
- 単体版を再生成、646ケースPASS。

### 2026-09-19 · 作り直した曲だけ再生ガイドを変更

- 再生ガイドが常に「作り直した曲」だったのを修正。生成完了時にテイクへ `adjusted` を記録し、微調整から生成した曲だけ「作り直した曲を　聴いてみる」、新規作曲は「聴いてみる」に分岐。テイク切替・取り消しでも対象曲に追従。
- 未生成・新規・微調整・元の曲へ戻す・再選択の5ケースを回帰検査に追加。646ケースPASS。820px・390pxで各2状態の表示を確認。単体版を再生成。

### 2026-09-19 · 保存ガイドを短縮

- 「作成した曲を、ここからダウンロード（保存ボタンを押す）」へ変更。33→27文字、自然幅389.6→322.4px。390px画面では296px幅の2行表示でボタンと重ならないことを確認。820pxでも確認済み。
- 単体版再生成、646ケースPASS。

### 2026-09-19 · OGP背景に薄い波形を追加

- ロゴの背後に青い波形を追加。不透明度16%、161本の縦線で音の強弱を表現し、文字は前面に維持。
- PNGは1200×630px、151,379→176,559 bytes。画像を目視して文字の読みやすさを確認。単体版再生成、646ケースPASS。

### 2026-09-19 · 利用先の案内を一般化

- 使い方の保存手順1か所を「ココフォリアなどにそのまま置けます。」から「ダウンロードした曲は、TRPGのセッションや動画などのBGMに使えます。」へ変更。特定サービスを前提にしない説明にした。
- 820px・390pxで使い方モーダルの折り返しを目視確認。単体版再生成、646ケースPASS。

### 2026-09-19 · 使い方にテイク上限を明記

- 「過去に作った曲は下の「テイク」に残ります。」の直後1か所へ「（最大６曲まで）」を追記。保持処理は変更なし（長い曲では6曲より少なくなる既存仕様も維持）。
- 820px・390pxでモーダル表示を確認。単体版再生成、646ケースPASS。

### 2026-09-19 · メロディの説明図を現行に合わせる

- 少なめの冒頭2枠を発音から休符へ、次の2枠を発音へ変更。冒頭の休符0→2拍で実装の開始位置に合わせた。図は1枠1拍の概念図で、音の並びは曲ごとに異なると明記。
- 820px・390pxで図と説明文を目視確認。単体版再生成、646ケースPASS。作曲処理の変更なし。

### 2026-09-19 · 作成と再生ボタンの横幅を統一

- 820px画面の作成181.7px／再生96.2pxを両方184pxへ統一。390px画面は作成324px／再生96.2pxから両方324pxへ。再生枠の左右余白をスマホ時だけ除き、親幅の差を解消。
- 両幅のスクリーンショットで折り返しを確認。単体版再生成、646ケースPASS。

### 2026-09-19 · MIT本文を単体版のHTMLコメントに同梱

- LICENSEのMIT本文（著作権表示・許諾文・免責文）をビルド時にhead内のコメントへ埋め込む。表示要素の追加0件。Markdownの区切り以降は対象外とし、コメントを壊す二重ハイフンを検査。
- MIT本文とコメントの一致を回帰検査に追加。単体版再生成、646ケースPASS。第三者素材の既存クレジットは維持。

### 2026-09-19 · フッターの素材表記を折りたたみに集約

- 常時表示の素材・ライセンス説明4段落を、初期状態で閉じた「使用素材・ライセンス」1入口に集約。既存クレジット・ソースリンク・単体版の同梱ライセンスは維持。MIT本文は従来どおりHTMLコメント内。
- 分割版・単体版×820px・390pxの4条件で初期閉状態、開閉、ソースリンク保持を確認。スクリーンショットで折り返しを確認し、単体版再生成、646ケースPASS。

### 2026-09-19 · 曲の自由利用案内を常時表示

- 曲の利用案内1段落を折りたたみ外へ移し常時表示。「使用素材・ライセンス」には第三者表記を残し、最後を「ツール本体のコードは MIT License。」に統一。MIT全文コメントは維持。
- 分割版・単体版×820px・390pxの4条件で開閉とソースリンク保持を確認。単体版再生成、646ケースPASS。

### 2026-09-19 · 調整と説明の入口を分離

- 開閉不具合ではなく、調整したい人が右端の「詳細」を押して説明だけを開く混同への対応。summary内の詳細ボタン1個を0個にし、開いた調整欄内へ「調整方法を読む」として移動。
- 820px・390pxで見出しから開く→説明を読む→閉じるの動線を確認。説明を閉じても調整欄が開いたままで作り直しボタンが表示されることを確認。単体版再生成、646ケースPASS。

### 2026-09-19 · 調整中だけ見出し右端に詳細を表示

- 説明ボタンを見出し右端へ戻し、閉状態では非表示、開状態だけ「詳細」を表示するCSSに変更。調整欄内の「調整方法を読む」は削除。
- 820px・390pxの各幅で閉状態0個→開状態1個→閉状態0個の表示を確認。詳細を押しても調整欄は閉じず、説明を閉じて戻れる。単体版再生成、646ケースPASS。

### 2026-09-19 · テイク最大12曲と再生・停止を追加

- 曲数上限6→12。音声メモリ上限24,000,000サンプル（ステレオFloat32で192MB）は維持。30秒相当の13テイクは新しい12曲を保持し、120秒相当は4曲に制限する回帰検査を追加。
- テイク欄に再生・停止を追加し、既存play/stopPlaybackと共通化。未生成・再生中・生成中・停止後の表示状態を検査。実生成した曲でテイク欄再生→停止、行選択→停止、上部再生→テイク欄停止を確認。
- 820px・390pxの表示を確認。単体版再生成、646ケースPASS。実音の聴感評価は未実施。

### 2026-09-19 · 再生と停止を各テイク内へ移動

- テイク欄の共通操作2個を削除し、各曲の枠内に再生・停止を追加。停止は該当曲が再生中のときだけ有効。クリック・キー操作が親行の再生処理へ伝播しないよう分離。
- 他曲再生時の停止無効化を回帰検査へ追加。実生成で曲内の再生・停止と上部再生からの停止を確認。820px・390pxで表示確認、単体版再生成、646ケースPASS。

### 第1次（コードレビューの指摘への対応）

| # | 内容 | 計測 |
|---|---|---|
| A-1 | テーマ長を尺から自動決定 | 50BPM×30秒で6/16小節しか鳴っていなかった |
| A-2 | 周ごとに4種の編成（そのまま/内声なし/内声を細かく/パッドなし） | 120秒の1周vs4周 相関 0.999 → 0.933 |
| A-3 | 生成の進捗表示（suspend/resume）と中止 | — |
| A-4〜A-9 | `crypto`裸参照、WAV保存の`<a>`未挿入、AudioContextのresume、base64の解放、空配列クラッシュ、合成ノイズの決定性 | — |
| A-10 | デッドコード削除、moodの`wave`を実際に使う | `wave`が音声生成で未使用だった |
| A-11 | `bgm_forge.html`を転送ページ化 | 完全コピー2枚の二重編集事故を防止 |
| B-1 | モチーフ生成の乱歩を反射に（下端が吸収壁だった） | 度0の出現 24.7%→10.3%、音域 4.13→6.17度 |
| B-3 | 旋律と内声の音域分離 | 1368曲で衝突0件、旋律の音域 11→15.3半音 |
| B-4 | パッドに150Hzハイパス＋マスターに260Hz/−2.6dBシェルフ | 80–250Hz帯 44.7%→17.8% |
| B-5 | 合成音源のフィルタ開放 | 600–1.5k帯 1.5%→4.8%、crest 3.51→4.40 |
| B-6 | ステレオ幅 | L/R相関 0.92→0.78 |
| B-7 | ループ用/終止ありの選択制（既定ループ用） | ループ点の落ち込み −4.7→−1.7dB |
| B-8 | 8分をタンバリンから合成ハイハットへ、キックにピッチエンベロープ | — |
| B-10 | 場面3種で旋律の扱いを変える | 探索=間引き、不穏=引き伸ばし、戦闘=刻みに割る |

### 第2次（聴いた感想への対応）

| 指摘 | 対応 | 計測 |
|---|---|---|
| 旋律がずっと鳴って自動作曲っぽい | 4小節を1句とする句構造。呼びかけ/息継ぎのある応答/再提示/締め。16小節では9〜12小節目を間奏に | 鳴っている時間 96%→51〜64%、1拍以上の休符 0→4〜9回/曲 |
| 跳躍の解決が古典則と違う | 3度以上跳んだら次は必ず反対方向へ順次進行。同方向3音で折り返し。終止も直前の音の近くから | 跳躍後の反行 15%→64%、最大跳躍 16→8半音 |
| 音源に幅がない | 3種→6種。倍音・包絡・フィルタ・残響を分離 | 15組中14組のスペクトル距離 31〜79 |
| 伴奏の展開が自動作曲っぽい | 小節ごとに形を選ぶ（パッド5形/ベース5形/内声5形/打楽器の抜きとフィル） | 前の小節と同型 98%→38〜57%、打楽器 50%→15% |
| 和音がワンパターン | B部の進行を別に、進行を3→6、和音に7th/9th/susの色 | 和音列 42→412種、後半が前半と同一 100%→0.4%、4音和音 33% |

### UI/UXの変更

- 設定パネル7枚→3枚に統合。ページ高さ 約3300px→1586px
- 雰囲気 6→14種（旋法にリディアン・ミクソリディアンを追加）
- テンポを体感ラベルに（とても遅い46／ゆっくり60／ふつう76／速め96／疾走116／めまぐるしい132）
- 長さ 3→8種、60秒以上は「1分」表記
- 終わり方をチェックボックス1つに
- メロディの量は「メロディを鳴らす」にチェックがある時だけ表示
- 生成ボタンを左寄せ、その右に「これから作る曲」の確認欄
- 保存ボタンは「聴いて確かめる」の1箇所のみ（2026-09-15に重複を削除）
- 「気に入らなければ磨く」→「作った曲を手直しする」、各ボタンに効果説明
- 生成中は全面モーダル（進捗％・内容・中止）
- 使い方モーダル（Esc/背景クリック/ボタンで閉じる）
- 見た目: 漆黒＋ヘアライン＋グリッド＋スキャンライン＋グレイン、曲指紋をコンソール風の SIGNAL LOG に、波形メーターに走査線と残像

### 第3次（2026-09-14〜15 · ループと操作性）

- **ループ尺をテーマ整数周にそろえた**（9-14）。76BPM・30秒指定は32拍＝25.263秒。
- **ループ最終小節をターンアラウンド化**（9-15）。継ぎ目直前300msが曲中平均の23〜34%まで落ちていたのを、
  他の小節線と同等（1.18〜2.18倍）まで回復。`hold` 音色のリリースを音符の後ろへ移したのが効いた。
- **曲指紋に `bpm` と `length` を追加**（9-15）。入っておらず、テンポ違いが同一曲と判定されて試聴キャッシュが誤配していた。
- **2小節試聴を追加**（9-15）。`BGMScore.sample()`。4か所にボタンとシークバー。設定を変えると自動で鳴る。
- **音声コンテキストをページで1つに**（9-15）。再生ごとに作ると自動再生制限で `resume()` が無期限保留になりうる。
- **少なめのメロディは2拍遅れて入る**（9-15）。オンにしたときの既定も「少なめ」に。
- **「いま ここ」ガイドを追加**（9-15）。今できる操作の枠が光る。一度保存すると以降は光らない。
- **テイク欄を行クリックで選択・自動再生**（9-15）。選んだ行の真下にSIGNAL LOGを表示。

詳細な原因と計測値は `CODE_REVIEW_V2.md` の「作業ログ 2026-09-15」に記載。

---

### 第4次（2026-09-16 · 単体起動版の取り込み）

`bgm_forge_standalone.html` だけが先に進んでいて、分割ソースが取り残されていた。
単体版を正として分割ソースへ戻し、作り直せる形にした。

| # | 内容 | 計測・根拠 |
|---|---|---|
| D-1 | `voiceChord` による声部進行（§2.5） | 移動 1.84→0.68半音、音域外 3→0音、広がり 8.54→11.85半音 |
| D-2 | `previewBars` の分離（§2.6） | 試聴がテーマ長を書き換えなくなった |
| D-3 | sus の三度衝突回避 | sus候補355小節のうち165小節（46%）が旋律の三度と重なっており、その分をplainへ退避 |
| D-4 | lamejs 未読込時にMP3ボタンを無効化 | 分割構成で `vendor/` を忘れた時に押せてしまっていた |
| D-5 | 「詳細」ボタンのクリックが `<summary>` の開閉を巻き込む問題 | `preventDefault`/`stopPropagation` |
| D-6 | 編集専用の詳細シート `d-edit` を追加、`d-listen` を再生・保存・調整の説明に書き直し | — |
| D-7 | `scripts/build_standalone.py` を追加 | 分割ソースから作り直した単体版が、配布中の単体版と**空行1つ以外バイト一致** |
| D-8 | `vendor/lamejs/lamejs-1.2.1.tgz` を復元 | 単体版にdata URIで埋まっていたLGPL用の原本一式。分割側になかった |

テストは `sample must be two bars` が `themeBars` を見ていたため落ちていた。`previewBars` を見るよう直し、
パッドの声部進行（重複音なし・和音1つあたり1.2半音以内）の退行テストを追加した。

---

### 第6次（2026-09-16 · 音色と場面の拡張）

音色 6→10種、場面 14→18種。合成音の追加なので**ファイルサイズは増えていない**。

| 追加音色 | 設計 | 倍音重心（基音=1） | 主役にした場面 |
|---|---|---|---|
| 木のマレット | マリンバ風。4倍音を混ぜ減衰を速く | 2.29 | 🧩 思索（ドリアン・76BPM・軽い打楽器） |
| オルゴール | 基音を弱め4倍音を主役に＝2オクターブ上に聞こえる | 5.15 | 🎠 まどろみ（イオニアン・46BPM・揺りかご型の進行） |
| オルガン | 整数倍音のみ・デチューンなし・持続 | 1.85 | 🕊️ 鎮魂（エオリアン・46BPM・VIIを使う旋法的進行） |
| ローファイ・テープ | 最も暗いフィルタ＋wow 7セント/5.4Hz | 1.32 | 📻 回想（ミクソリディアン・60BPM） |

既存: synth 1.51 / glass 1.91 / pluck 1.69 / drone 1.07。持続系がアンビエント1つしかなかったので、
輪郭のはっきりした持続音（オルガン）を足したのが穴埋めになっている。

新場面は既存14場面と**和音進行が1つも重複しない**ことを確認済み（既存同士には重複がある。例: 明るいと凱旋は4つ共有）。
18場面すべてが別の曲指紋になることも確認した。`?v=20260916j`。

**未確認**: 4音色・4場面とも鳴らしていない。オルゴールは基音を弱める作りなので低音域で細くなる可能性がある。

---

### 第5次（2026-09-16 · 公開データとの突き合わせ）

`automatic_composition_research_data.md` に挙がっていた公開データのうち、JSB Chorales と Nottingham は
GitHub 経由で取得でき、Groove は配布元が遮断されているため手動でダウンロードしてもらった。
生成結果と人間の曲を同じ指標で並べたのが `RESEARCH_GAP_REPORT.md`。計測スクリプトは `scripts/research/`。

そのうち打楽器だけを実装に反映した（§2.7）。

| 指標 | 人間（Groove） | 旧 | 新 |
|---|---|---|---|
| velocityのばらつき（キック/スネア/ハット） | 32.6 / 40.3 / 28.4 | 12.1 / 4.2 / 7.0 | 18.3 / 15.2 / 14.9 |
| グリッドからのずれ | ±21ms（系統ずれ除去後） | 0ms | 平均7.1ms・std 8.7ms |

**まだ反映していない差**（レポートの上位3件）: 旋律の音域が人間の半分（8.3対16.9半音）、
跳躍後の解決が硬すぎる（69%対20.6%）、同音が多い（18.4%対11.4%）。この3つは同じ原因でつながっている。

### 第7次（2026-09-18〜19 · 聴いた感想への対応と、iOS・画面の整備）

リポジトリへ移って最初のまとまった作業。**すべて実際に鳴らした感想が起点**で、計測はそのあとに回している。

#### 音楽面

| # | 指摘 | 対応 | 計測 |
|---|---|---|---|
| G-1 | 幻想・夜空の伴奏がうるさい | `ACCOMP_TRIM` を新設（§2.8）。夜空を息継ぎフェードの対象に追加（フロア .10） | 夜空は和音が一定音量で鳴りっぱなしだった（幻想・鎮魂だけが対象になっていた） |
| G-2 | 「穏やか」に個性がない | 「🌾 のどか」へ。エオリア→ミクソリディア、hush→walk、7.8拍の持続和音→歩くベースと爪弾き | 和音の移り変わり 16小節で2回→12回、音域 65→71 |
| G-3 | カントリーらしい音がない／爪弾きは使いすぎ | 音色「スチール弦」を新設。倍音6本を数セントずつ逆にずらして弦のうなりを作り、撥弦の瞬間だけ基音の18倍までローパスを開く | 減衰 .04→.09、余韻 .14→.38秒。音量は爪弾きと揃えた |
| G-4 | 「まどろみ」の使い道が怪しい（CoC/エモクロア用途） | 「🔍 疑惑」へ入れ替え。低音を踏み、上の声部が動く。ドリア・60拍・ticks・ローファイテープ | 暗いは16小節で和音1回、疑惑は8回。調査パート用の穴が埋まった |
| G-5 | 疑惑は行き来するアルペジオのほうがよい | `autoPattern` を新設（§2.9）し、疑惑の既定を `'wave'` に | 先に「裏拍だけで行き来する線」を試したが、聴いた判断で内蔵の型に戻した |
| G-6 | 幻想のループが気になる | 和音を2小節ごと→毎小節、持続 2.6→4.2拍、息継ぎのフロア .12→.26 | 音量のばらつき 0.79〜0.84→0.35〜0.44、最小/最大 0.01〜0.06→0.24〜0.29（夜空は約0.19） |

G-6の原因は2つ重なっていた。幻想は60拍・30秒指定だと**4小節16秒**のループ（夜空25秒・のどか20秒より短い）で、
その中で音量が最大の1〜6%まで落ちてから戻る波を2回繰り返していた。包絡を16分割して数字で出すと `9742211197421131`。
16秒という長さ自体には手を付けていない（§6）。

#### 音源

| # | 内容 | 根拠 |
|---|---|---|
| G-7 | 音色「フルート」を追加（VSCO-2-CE `Woodwinds/Flute/susvib` から4音） | 原本のピークが0.05〜0.21しかなく、ピーク調整をしないとほぼ聞こえない。**ファイル名のオクターブ表記が実音と1つずれる**ため、4音とも波形から基本周波数を測ってMIDI音高を決めた（弦にも同じ問題があり manifest で補正済み） |
| G-8 | 音色「生楽器」→「室内楽」に改名 | 他の音色名は聞こえ方を表すのに、ここだけ作り方の分類だった |

**ギターは同梱できなかった。** VSCO-2-CE はオーケストラ音源でギターを含まず、同じ作者の VCSL（CC0）にも
ギター・バンジョー・マンドリンはない（ストラムスティックはある）。合成の「スチール弦」で代用している。

単体版は 16.6MB → 18.0MB。

#### iOS

「スマホで作成された曲が再生できない」という報告。**原因はiPhoneのサイレントスイッチ**だった（Web Audioは
着信スイッチで無音になる。動画や `<audio>` の音は鳴るので気づきにくい）。アプリ側の不具合ではなかったが、
調べる過程で実際の穴が2つ見つかったので塞いだ。

- `new AudioContext()` だけで `webkitAudioContext` の退避がなかった。iOS 14.5より前のSafariでは何も鳴らない
- 復帰判定が `state==='suspended'` だけだった。iOSは着信や他アプリで `'interrupted'` を取るため、
  一度割り込まれると戻れなかった。`state!=='running'` に変更

#### 画面

- 「再生」を「新しい曲を作る」と同じ大きさに。保存3つを金色の主役に。「この曲を調整」は控えめに
- 調整を開くと枠・見出し・仕上げボタンが**調整専用の赤**（`--edit:#e0705f`）になり「調整中」の印が付く。
  仕上げの「この設定で曲を作り直す」も同じ赤にして、赤い区画の出口がそこだと示す
- 試聴ボタンの待機中の文言が、HTMLに書いた文ではなく**JSの「▶ 試聴」で上書きされていた**。
  両方を「▶ 変更した設定で2小節聴く」に統一
- テイク欄に「3 / 6 曲　保持できるテイクは6曲までです」と作成日時。見出しは `takeTitle()` に共通化し、
  「聴いて確かめる」の見出しにも同じ文字列を出す
- 見出しの一文を「場面に合ったBGMを、ブラウザでつくれる。」に変更

#### 自分で持ち込んだ不具合

のどかの既定テンポを92拍にしたが、`TEMPOS` は 46/60/76/96/116/132 の6段で**92がない**。
選択欄は一覧にない値が来ると「その値だけの選択肢」を足す作りなので、名前のない「92 BPM」が1つ紛れ込み、
確認欄でも他が「ふつう 76 BPM」なのに、のどかだけ名前なしで出ていた。96へ寄せて解消
（8小節・20秒のループで性格は変わらない）。**既定BPMは必ず `TEMPOS` に含まれる値から選ぶこと。**

---

---

## 4. 作業のやり方（これを踏襲してほしい）

このプロジェクトで効果があったのは、**感想を数字に変えてから直す**という手順。

1. **測る** — 「〜っぽい」という指摘を、楽譜の統計かレンダリング結果の数値に翻訳する
   - 楽譜側: `node` で数百〜数千曲生成して分布を出す（`/tmp/*.cjs` に使い捨てで書いた）
   - 音声側: ヘッドレスChromiumで実レンダリング→FFTで帯域比、RMS、crest、L/R相関、周期相関
2. **直す**
3. **測り直す** — 改善したことを同じ指標で示す
4. **テストに固定する** — 同じ退行が起きないアサートを `tests/scene-variation.cjs` に足す
5. 変更内容と計測値を、この `HANDOVER.md` の §3 に追記する（`CODE_REVIEW_V2.md` は旧フォルダに残置）

第7次では、この手順を**聴いた感想 → 計測 → 直す → 測り直す**の順で回した。指摘が音楽の印象（「うるさい」
「ループが気になる」）なので、先に数字へ翻訳してから触っている。有効だった道具を2つ挙げておく。

- **ブラウザ上でのA/B** — 公開中のページで `window.BGM_TEST={}` を定義してから `bgm-forge.js` の**文字列を
  書き換えて** `new Function()` で評価すると、UIを初期化せずにAPIだけ生える。定数を変えた版を何通りも
  その場でレンダリングして比較できる（幻想のループは4案を比べて決めた）。レンダリングは1曲で数十秒かかるので、
  結果を `window` に溜める非同期処理にして、あとから取り出すこと。
- **見た目はスクリーンショットで確認する** — ヘッドレスChromiumで `bgm_forge.html` を開き、テイク行などを
  JSで流し込んでから撮る。820pxと390pxの2幅で見ると、折り返しの事故に気づける。

「たぶん良くなった」で終わらせないこと。実際、内声を絶対音域で固定したら80–250Hz帯がかえって増えた（41.8%→51.7%）ことが計測で分かって方針を変えた、という失敗もしている。

### 計測スクリプトの雛形

```js
// 楽譜の統計（bgm-score.js は Node から直接 require できる）
const S=require('./bgm-score.js');
const fs=require('fs'),vm=require('vm');
const sb={BGMScore:S,Blob,console,setTimeout,window:{BGM_TEST:{}}};vm.createContext(sb);
vm.runInContext(fs.readFileSync('bgm-forge.js','utf8'),sb);
const api=sb.window.BGM_TEST;   // MOODS/MODES/TEMPOS/LENGTHS/SOUNDS と compose/render/midiFile が取れる
for(const m of api.MOODS){ const s=S.compose({mood:m,scale:api.MODES[m.mode],bpm:96,length:60},seed); ... }
```

音声の実測はヘッドレスChromiumが要る。`window.BGM_TEST={}` を先に定義したページで
`bgm-forge.js` を読むとUIを初期化せずAPIだけ生えるので、`BGM_TEST.render(score)` でレンダリングできる。

---

## 5. 既知の制約・未対応

- **同梱バンクは1ページにつき1回しか復号できない**。`sampleBuffers` が復号後に `meta.pcm` を
  `delete` するため。いまは `sampleCache` を捨てないので実害はないが、キャッシュを作り直す経路を
  足すと `atob(undefined)` で落ちる。
- **原音（Freesoundからの4ファイル）はリポジトリに入っていない**。`.gitignore` 済み。
  箏の原音は448MBで、GitHubは100MB超を拒否するため `git add -A` すると push ごと失敗する。
  音源を作り直すときは各 `CREDITS.md` の出典から再ダウンロードすること。
- **実録音は4種**（シタール・箏・笛・合唱）。他の音色はすべて合成。
  合唱だけがループする音源で、音符の中の減衰（`SUSTAIN_FADE`）が要る唯一の存在。
- **RMSでは持続音と減衰音の聞こえ方を比べられない**。合唱の音量を調べたとき、
  RMS比では儀式(+6.4dB)が幻想(+12.8dB)より低く出たが、実際にうるさかったのは儀式だった。
  「素材そのもののRMSと減衰の速さ」を見るほうが当たる（§3 2026-09-20）。
- **度数と半音を混同しないこと**。`pitch(scale,d)` は音階の音数で意味が変わり、
  5音音階では4度＝9半音になる。跳躍の制限は半音に直したが、`chordDegrees` は度数のまま。

- **長い曲のレンダリングが重い**。tense/132BPM/120秒でヘッドレスChromium約84秒（実機Macはもっと速い）。進捗と中止で体感は改善したが、根本的には速くなっていない。
  2026-09-16に**選べる長さの上限を120秒にした**（180秒・240秒を廃止）ので最悪ケースは半分になったが、120秒が重いこと自体は変わっていない。ループ用と割り切って30〜60秒で作るのが実用的。
- ~~戦闘/132BPM/120秒でループ点に約50msの薄い箇所~~ → 2026-09-15に解消。ループ最終小節をターンアラウンド化し、リリースを音符の後ろへ移した。全14場面×4種で継ぎ目が通常の小節線と同等以上（詳細は `CODE_REVIEW_V2.md` の 2026-09-15 の節）。
- **聴感評価をしていない**。計測上は整っているが、鳴らして音楽的に良いかの判断は行っていない。
- **実際の発音を確認できていない環境がある**。裏タブでは `navigator.userActivation.hasBeenActive` がfalseのままで自動再生制限がかかる。UIの状態遷移までは確認済み。
- **iPhone Safari で使われている**（2026-09-19に利用報告）。Web Audioは**着信スイッチがサイレント側だと無音**になり、
  画面上はメーターも動くので壊れて見える。これはブラウザの仕様でアプリ側では検知できない。困ったら最初にここを疑う。
- **Firefox・Android実機は未検証**。`@property`（ガイドの光）と `mask-composite` を使っている。
- **46BPM×20秒だけテーマが2小節**（I–Vのヴァンプ）。
- **Locrianの恐怖は主和音が減三和音のまま**。意図的。
- **声と重ねた実地確認をしていない**。260Hzシェルフ−2.6dBは計測で決めた値。実際にセッション音声と重ねてまだ覆うようなら−4dBまで深くする余地がある（一度−4にしたら低域が痩せすぎて戻した経緯あり）。
- テイクはページ再読み込みで消える（保存は明示的に）。上限12曲。13曲目で古いものから落ちる（音声メモリ上限により長い曲では少なくなる）。
- **テストが `tests/scene-variation.cjs` の1本だけになっている**。旧フォルダにあった playback / adjust / browser / loop /
  seam はリポジトリへ移していない。再生まわりやループ継ぎ目を触るなら `inside-rooms/tests/` から持ってくること。
- **幻想のループが16秒**（4小節）。60拍・30秒指定だと4小節しか入らない。音量の谷は浅くしたが（§3 第7次 G-6）、
  短さ自体は残っている。テンポを上げるか既定の長さを延ばすかで、どちらも性格が変わる。
- **ギターの音源がない**。VSCO-2-CE にも VCSL にも入っていない。合成の「スチール弦」で代用中。
- **`git` のロックファイルを消せない環境がある**。Cowork経由で触ると `.git/index.lock` などが残るため、
  `.git/_stale_locks/` へ退避している。中身は空のゴミなので、フォルダごと捨ててよい。
- **単体版は20.8MB**（2026-09-20時点）。音源12.9MBとMP3エンコーダー原本3MBを含むため、メール添付には向かない。開くのにも数秒かかる。音色を足すたびに増える。
- **単体版と分割ソースの同期はビルドの手作業に依存する**。CIはない。単体版だけを直接直すと2026-09-16と同じ乖離が起きる。
- **打楽器の揺れは聴いて決めていない**。Grooveの3分の1という倍率は、会話に重ねる用途からの推定であって計測ではない。
- **Groove MIDI Dataset は CC BY 4.0**。統計しか使っていないが、画面と `BGM_FORGE_README.md` にクレジットを出している。消さないこと。
- **`voiceChord` は総当たり探索**。和音構成音3〜4音×音域内のオクターブなので実用上は軽いが、構成音を増やすなら枝刈りが要る。

---

## 6. 今後の改善案（効きそうな順）

### 6.0 次にやること（2026-09-20 夜の時点）

**耳の判断待ち（コードは入っている。鳴らして可否を返すだけ）**

| 対象 | 現在の値 | 触る場所 | 一段下げる／上げる |
|---|---|---|---|
| 合唱が大きい／小さい | 係数 `.25`・減衰 `.33` | `bgm-forge.js` の `partTrim` と `SUSTAIN_FADE` | 表は §3「合唱をもう一段下げる」 |
| 儀式の持続音の引き方 | `FADE.ritual=[.12,3]` | 同 `FADE` | 1つ目＝どこまで、2つ目＝何拍で |
| 和風の旋律 | 跳躍は最大7半音 | `bgm-score.js` の `semis(...)>7` | 残り7.9%は五音音階そのものの性質 |
| 箏の切り出し | 6音・各1.5秒 | `scripts/build_koto.py` の `WANT` / `CROP` | 作り直しは同スクリプト |
| 箏の早回しの折り返し | `SAMPLE_LIFT.koto=7` | `bgm-forge.js` | 下げるほど早くオクターブ下げる |

**手つかずで、やるなら次**

0. **箏の弦らしさが薄い**（2026-09-20、採用したうえでの残課題）。収録を2.5秒→1.5秒に縮めた
   ぶん、長く伸ばす音の余韻が短い。`CROP` を戻すのは不可（原音が連続即興なので、2.5秒だと
   別の弦が混ざる。§3参照）。取れる手は3つ。
   (a) 減衰する撥弦なので、収録の末尾0.45秒のフェードを合成した余韻で延長する
       （最後の1周期をループさせて指数減衰させる。`build_koto.py` 側の仕事）
   (b) 旋律の56%が最高音 root 69.5 を +1.5〜6.5半音で早回ししている。早回しは収録を縮めるので
       余韻がさらに短くなる。和風の旋律の音域を下げれば早回しが減る（`bgm-score.js`）
   (c) `sampleNote` の `release`（箏は1秒）を伸ばしても、収録が尽きたあとは無音なので効かない。
       ここを触っても直らないことだけ覚えておくこと
1. **儀式の「前半に旋律が入らない」**。既定の30秒指定は実尺16秒＝テーマ4小節で、`minimal` だと
   前半 part0＝0音・後半4音になり、波形の中央に段差が出る。60秒以上なら両方に入る。
   直すなら `makeMelody` の `shape` 判定。指摘は受けたが未着手。
2. **五音音階の和音構成音が26半音に広がる**。`chordDegrees` の ±7/±5/±3 は7音音階前提の度数。
   旋律の跳躍は半音で抑えたが、和音の取り方は度数のまま。和風でまだ外れて聞こえるならここ。
3. **`ACCOMP_TRIM` に儀式が入っていない**（伴奏1.0のまま。幻想.72/夜空.62/疑惑.68）。
   旋律が実録音の合唱になったので、伴奏を下げて声を通す余地がある。
4. **説明文の積み残し**（§3 2026-09-19）。疑惑↔思索の「調査」、荘厳↔鎮魂の「葬送」、
   悲哀の例に入った「回想」、鎮魂と凱旋が自分自身の名前を例に入れている点。
   2026-09-20 に神楽の「和風の儀式」→「巫女の舞」を済ませた（儀式という場面が別にあるため）。
   **残っているのは鎮魂の「儀式としての弔い」**。これも儀式という場面名と重なっている。
   なお神楽と儀式は和音進行・旋法・打楽器が同一で、音色・主音・エネルギーだけが違う双子。
   説明文で区別を付けているので、文面をいじるときはこの近さを意識すること。
5. ~~篠笛の実録音がない~~ → **2026-09-20 に実録音へ差し替えた**（§3）。CC0の篠笛は存在しないので、
   CC0の「何の笛か不明なオリエンタル風の笛」（Freesound 553217）を採った。残りは2つ。
   (a) 画面の音色名「神楽笛」は録音についての主張ではなく狙いの呼び名。正確さを優先するなら変える。
   (b) 笛が鳴るのは25秒中10.1秒（40%）。旋律を出せば増えるが、`lead` を立てると内声が MIDI 47
       まで下がって笛として成立しない。やるなら「旋律だけ実録音、内声は合成」に作り分ける。
6. ~~神楽鈴が無い~~ → **2026-09-20 に合成で入れた**（§3）。CC0に神楽鈴は無いので合成にした。
   摘みは `SUZU` 表（粒の数・撒く幅・偏り・帯域・左右）、音量は `.9*drumGain`。
7. ~~民族からシタールがほとんど消えている~~ → **2026-09-20 に内声を奇数小節で戻した**（§3）。
   シタールは1曲6.0音→17.0音（全体の12%→27%）。じゃらん直後3秒の内声は0.00秒のままなので
   「コードが続く」は再発していない。増減させたいときの数字は §3 の表にある。

**返事待ち：テイクの保存・インポート・エクスポートは要るか**

2026-09-20 に「検討しています」と言われたまま、まだ決めていない。こちらの見立てを置いておく。

- **音声そのものの書き出しは要らない**と思う。WAV／MP3／MIDI の保存がすでにあり、
  テイクの音声を別形式で持ち回る意味が薄い。12曲の音声をブラウザに持たせると重くもなる。
- 代わりに**「曲のコード」**を提案した。`identity()` の指紋があれば同じ曲が完全に再現できるので、
  短い文字列を貼るだけで曲が戻る。長さの実測は **指紋そのままで112文字**、
  場面や音色を読める形にした簡易JSONで601文字。前者を採り、
  「コードをコピー」「コードから復元」の2つを付けるのが最小の形。
- そのうえで気に入ったコードだけを `localStorage` に残す（音声は残さない）なら、
  ページを閉じても消えない“お気に入り”になる。**音声を保存する話とは別物**なので、
  やるなら分けて考えること。

要る／要らない、要るならどの形か、の返事が来たら着手する。

**アイアールさんの手が要ること：「意見を送る」を動かす**

Workerの公開と送信先URLの設定は完了（2026-09-20）。Webhook登録も完了。ページ公開と実送信確認が残っている。以下は初回設定手順。

1. Worker を置く（リポジトリ直下で）

   ```sh
   cd worker
   npx wrangler deploy                      # 初回はブラウザでCloudflareにログイン
   npx wrangler secret put DISCORD_WEBHOOK  # 聞かれたらWebhook URLを貼る
   ```

   **Webhook URL はチャットにも、このリポジトリにも貼らないでください。** 会話にも履歴にも残ります。
   入れる先は上の `secret put` が聞いてくるところだけです。

2. `deploy` が表示するURL（`https://bgm-forge-feedback.<アカウント>.workers.dev`）を、
   `bgm_forge.html` の `<meta name="feedback-endpoint" content="">` に入れる。
   入れたら `python3 scripts/build_standalone.py` → `node tests/scene-variation.cjs`。

3. 公開（`公開する.command`）。ページを開いて、ヘッダに「✉ 意見を送る」が出れば通っている。
   届かないときは `npx wrangler tail` で Worker のログを見る。

`worker/wrangler.toml` の `ALLOWED_ORIGINS` は GitHub Pages のドメインだけを許している。
独自ドメインや Cloudflare Pages へ移すときは、ここに足さないと403で弾かれる。

**本体のCloudflare移行は2026-09-20に完了**（PagesではなくWorkers Static Assets）。旧記録：
本体は `bgm-forge.suihei.workers.dev`、中継は `bgm-forge-feedback.suihei.workers.dev` に分離。独自ドメインは未設定。

**やってみて却下されたこと（再導入しないこと）**

- **シタールの「じゃらん」（撥弦を駆け上げる飾り）**。2026-09-20 に入れて同日に撤去（§3）。
  4回直して、重なり・長さ・間隔はすべて計測で潰したが、「小節0と小節4にきっかり、毎回同じ形で
  現れる6音」という規則性が最後まで機械の目印に聞こえた。**音の作りではなく置き方の問題**なので、
  同じ形で作り直しても同じ結果になる。異国情緒はヒジャーズ旋法・持続低音・内声のアルペジオが
  担っている。もし再挑戦するなら、独立した仕掛けではなく**内声の書き出しに溶かす**方向で。

- **息継ぎ（`breathe`）**。2小節ごとに全パートを無音にする仕組み。計測では効いていた
  （無音率 1.3%→9.4〜16.4%、実音の谷 −27.7dB）が、聴くと「息継ぎ後の長い音がかえってうるさい」。
  仕掛けごと撤去済み。本当の原因は持続音がずっと同じ音量だったことで、`FADE` で解決した。

---


### 6.1 音楽面

0. **書き出し時のラウドネス正規化（いちばん効きそう）** — 場面ごとに出来上がりの音量が違うのを、`ACCOMP_TRIM` という
   手当てで場面別に押さえている。ITU-R BS.1770 / EBU R128 の積分ラウドネス（LUFS）を測って全場面を同じ目標に
   合わせれば、`buses.master.gain.value=.72` の固定値ごと置き換えられ、場面を足すたびに手で調整する必要がなくなる。
   データセットは要らず、今のコードに足せる。
1. **パッドとベースの音色を分ける** — `sampleKinds`（§2.10）でフルートだけ実現した。表を音色ごとに持たせれば
   「弦なし・ピアノだけ」「オルガン的」なども同じ仕組みで足せる。合成音色側（`VOICES`）は未対応。
2. **リズムの単位を変える** — 現状すべて4/4。6/8や3/4を足すと「儀式」「幻想」「悲哀」の性格が立つ。`RHYTHMS` と `events()` の拍計算に拍子を通す必要があり、影響範囲は中くらい。
3. **転調** — 長尺（2分以上）で3周目に全音上げ、など。`events()` の周回ループで `root` にオフセットを足すだけで実装できるが、テーマ共有の意味づけとMIDIの調号をどうするか要検討。
4. **低音の連結** — パッドは `voiceChord` で声部進行するようになった（§2.5）が、ベースは今も常に根音（＋経過音）。第1転回（3度が最低音）を混ぜると進行が滑らかになる。
5. **旋律のモチーフ展開** — 現状はモチーフを8音固定で使い回している。反行形・逆行形・拡大縮小を後半で使うと「作曲している」感が出る。`makeMelody` の `cell` の取り方を変えるだけ。
6. **場面3種の打楽器をもっと変える** — 探索は打楽器なし、不穏は心音、戦闘はドライブ、で固定。もう少し段階が欲しい。
7. **旋法ごとの7th/9th/susの重み調整** — `bgm-score.js` の `COLOUR` テーブル。聴いて多すぎ/少なすぎを直す。
8. **ステム音量の実測をデータから取る** — Slakh2100（CC BY 4.0・2,100曲・34の楽器クラス・ステム付き）で
   「伴奏はメロディの何dB下か」の分布が出せる。Groove MIDI と同じく統計だけ使う形にできる。
   MUSDB18（4ステム）と The Mix Evaluation Dataset（DAWセッション＋人の評価）は NonCommercial なので扱いが別。
9. **幻想のループを長くする** — 4小節16秒。テンポを76にすると8小節25秒（夜空と同じ）。性格が変わるので要相談。
10. **リバーブを実測IRに** — OpenAIR（ヨーク大学）の実空間インパルス応答。今は `spaceBus` で人工的に作っている。
   荘厳とのどかで残響の質そのものを変えられる。

### 6.2 機能面

1. **テイクの永続化** — `localStorage` にスコア（音声ではなく設計データ）だけ保存すれば、再読み込み後に再生成できる。音声は数十MBあるので保存できないが、スコアはJSONで数KB。
2. **シードの入力欄** — 気に入った曲のシードを手で入れて再現できるようにする。テイク欄にシードは出ているのに入力口がない。
3. **A/B比較の同時再生位置合わせ** — テイクを切り替えたとき同じ再生位置から続ける。
4. **ステム書き出し** — 旋律/パッド/ベース/内声/打楽器を別WAVで出す。動画編集で使いやすい。`render()` をパートごとに呼ぶだけ。
5. **ループポイント埋め込み** — WAVの `smpl` チャンクにループ点を書く。対応プレイヤーならギャップレスになる。
6. **プリセットの保存** — よく使う設定の組み合わせに名前を付けて保存。

### 6.3 やらないと決めたこと

- DAW相当の編集、MIDI入力、歌声生成（仕様書の対象外）
- 外部API・学習モデルの利用（ブラウザ単体で完結させる方針）

---

## 7. 別チャットで再開するときの手順

1. リポジトリのフォルダを作業フォルダとして渡す
   （`~/Documents/Codex/2026-09-17/documents-plugin-documents-openai-primary-runtime/work/bgm-forge`）
2. `AGENTS.md`（開発ルール）と、この `HANDOVER.md` の **§0（現状）と §6.0（次にやること）** を読んでもらう
3. `node tests/scene-variation.cjs` がPASSすることを確認してから着手。
   **落ちたら、まず原因を報告してもらう**（たいていは単体版の作り直し忘れか `?v=` のずれで、
   落ちたメッセージが直し方をそのまま書いている）
4. **直すのは分割ソース**（`bgm-score.js` / `bgm-forge.js` / `bgm_forge.html`）。単体版は触らない
5. 変更したら `bgm_forge.html` の `<script src="...?v=YYYYMMDD-名前-ハッシュ">` の版を上げる。
   ハッシュは手で計算しない。テストが落ちて貼るべき文字列を表示する
6. `python3 scripts/build_standalone.py` で単体版を作り直す
7. テストを回す。画面を変えたらヘッドレスChromiumでスクリーンショットを撮って目で見る（§4）。
   音を変えたらヘッドレスChromiumで `BGM_TEST.render()` を呼び、20ms窓のRMS包絡で測る（§3の各回）
8. `HANDOVER.md` の §3 に計測値つきで追記する
9. `git add -A && git commit`（日本語のメッセージ・mainに直接。PRは作らない）
10. 公開はリポジトリ直下の `公開する.command` をダブルクリック。反映まで1〜2分
11. 「〜っぽい」という指摘には、まず計測スクリプトを書いて数字を出してから手を入れる

**環境について知っておくこと**

- **`git push` を代行できない環境がある**。GitHubの認証情報に触れないため
  `could not read Username for 'https://github.com'` で止まる。その場合は10を案内する。
- **`.git` のロックファイルを消せない環境がある**。`.git/_stale_locks/` へ退避する運用。
  中身は空のゴミなのでフォルダごと捨ててよい。
- **ファイルを削除できない環境がある**。`git checkout -- <file>` が
  `unable to unlink` で失敗する。`git show HEAD:<file> > <file>` なら上書きなので通る。
- **複数のセッションが同時に触ることがある**。着手前に `git status` と
  `git log --oneline -3`、ファイルの更新時刻を見て、他所の作業が走っていないか確かめること。
  実際に2026-09-20、計測の最中に別セッションが4場面と4音色を足していた。

### 引き継ぎ時の一言テンプレート

> `work/bgm-forge` のBGM Forgeを続けたい。`AGENTS.md` に開発ルール、`HANDOVER.md` の
> §0 に現状、§6.0 に次にやることがある。作業前に `node tests/scene-variation.cjs` を回して
> 現状を確認してほしい。直すのは分割ソースで、最後に `scripts/build_standalone.py` で
> 単体版を作り直し、`?v=` の版も上げること。コミットは日本語でmainに直接、PRは作らないで。
> 「〜っぽい」という指摘をしたら、まず計測してから直す進め方でお願いします。
> 音の良し悪しは判定しなくていい。計測して「ここまでは言える」を示して、最後は鳴らして確かめる。
